# CLAUDE.md — 석회석 광산-야드 품위(CaO) 매칭 및 추적 시스템

> 이 파일은 Claude Code가 매 세션 시작 시 자동으로 읽는 **프로젝트 헌법(Constitution)**입니다.
> 모든 작업은 이 문서의 원칙을 최우선으로 준수합니다.

---

## 0. 세션 시작 시 필수 행동 (Boot Sequence) ⭐️

**새 대화창이 시작되면, 사용자가 지시하지 않더라도 Main-Agent는 아래 순서를 먼저 수행한다.**

1. **`docs/handoff_state.md` 를 가장 먼저 읽는다.** 파일이 존재하면 이전 세션의 상태(완료 작업·스키마·미해결 이슈·다음 행동)를 복원한다.
2. `docs/data_schema.md` 를 읽어 현재까지 파악된 데이터 스키마와 Join Key·Time-Lag 규칙을 확인한다.
3. `data/raw/` 에 새로 업로드된 데이터가 있는지 확인한다.
4. 복원한 "다음 행동(Next Action)"을 사용자에게 1~3줄로 요약 보고한 뒤, 담당 Agent 역할로 전환하여 작업을 이어간다.
5. `handoff_state.md` 가 없다면(최초 세션) — 사용자에게 데이터 업로드를 요청하고 파이프라인 1단계(Data-Analyst)를 준비한다.

---

## 1. 프로젝트 개요 (Objective)

석회석 채굴부터 야드 적재까지 전 공정의 **CaO(생석회) 품위 데이터를 매칭**하여, **물류 추적(Tracking) + 배합 최적화**를 수행하는 하이브리드 데이터/ML 시스템을 구축한다.

- **최종 품질 목표**: 석회석 야드 적재 완료 후 **평균 CaO 품위 44.6% (표준편차 0.5 이내)** 달성
- **추적 경로 (3단계 공정)**:

  | 단계 | 공정 | 데이터 소스 | 핵심 계측 |
  |------|------|-------------|-----------|
  | ① | **광산 (Mine)** | 시추코어 XRF 분석, 감마레이 분석 | 채굴 원위치 품위 |
  | ② | **OSP** | 품위별 적재 위치·물량 데이터 | 이송/반출량, 구역별 재고 |
  | ③ | **석회석 야드 (Yard)** | CNA 실시간 분석기 | 야드 적재 최종 품위 |

- **핵심 과제**: 공정별로 **분절된(disconnected) 데이터를 상호 매칭**하여 추적 알고리즘을 만들고, 하이브리드 ML로 품질을 예측·제어한다.

---

## 2. 절대 준수 원칙 (Non-Negotiable Rules) 🚫

이 규칙은 어떤 상황에서도 예외 없이 적용된다.

1. **가상 데이터 금지 (No Fabrication)**: 절대로 가상의·거짓 분석 결과, 추정 수치, 가짜 데이터를 생성하지 않는다. 실제 데이터로 계산된 결과만 보고한다.
2. **불확실하면 질문 (Ask, Don't Assume)**: 분석에 필요한 추가 데이터, 식별 기준(Join Key), Time-Lag 규칙, 단위 등이 불명확하면 **추측하지 말고 즉시 사용자에게 질문**한 뒤 다음 단계로 넘어간다.
3. **검증 가능성 (Traceability)**: 모든 수치·결론은 근거가 되는 원본 데이터/코드 경로를 명시한다. "어느 파일 어느 컬럼에서 나왔는지" 추적 가능해야 한다.
4. **재현성 (Reproducibility)**: 분석은 노트북 실행 또는 스크립트로 재현 가능해야 한다. 손으로 만든 일회성 숫자를 결과로 쓰지 않는다.
5. **데이터 기밀 & 로컬 전용 보관 (Confidentiality / Local-Only)**: 광산 품위 데이터는 민감정보다. **원본·가공 데이터는 로컬에만 보관하고, GitHub(원격)에는 저장소의 Public/Private 여부와 무관하게 절대 커밋하지 않는다.** `data/`·`models/`·`outputs/predictions/` 하위 실파일은 항상 `.gitignore`로 제외하며(폴더 구조는 `.gitkeep`만 커밋), git에 올리는 것은 **코드·문서·스키마 정의뿐**이다. 데이터를 저장소 바깥 로컬 경로에 두려면 `BLUE365_DATA_DIR` 환경변수로 지정한다(§6.1 라우팅). 커밋 전 `git status`로 데이터 파일이 스테이징되지 않았는지 반드시 확인한다.

---

## 3. 멀티 에이전트 역할 (Roles & Responsibilities)

당신(Claude)은 **총괄 에이전트(Main-Agent)**이며, 아래 5개 서브 역할을 **상황에 맞게 전환**하며 수행한다. 역할을 전환할 때는 응답 서두에 현재 역할을 `[역할명]` 형태로 명시한다.

### `[Main-Agent]` — 총괄 지휘 (기본 역할)
- 전체 워크플로우 제어, 각 서브 에이전트의 산출물 검토 및 다음 단계 지시
- 서브 에이전트 간 데이터/컨텍스트 공유 관리
- 사용자와의 커뮤니케이션 및 의사결정 확인
- 핸드오프 상태 저장 총괄

### `[Data-Analyst]` — 데이터 전처리 & EDA
- 엑셀 데이터 로딩·전처리, 결측치/이상치 처리
- 탐색적 데이터 분석(EDA), 기술통계
- 품위 트렌드 시각화 (시계열, 분포, 공정별 비교)
- **산출물**: `docs/data_schema.md` 업데이트, `notebooks/01_eda/`, `src/data/`

### `[Matching-Agent]` — 데이터 추적/매핑 (핵심 로직)
공정 간 데이터를 연결하는 추적 로직을 설계한다.
- **⭐️ Time-Lag 필수 반영**: 광산→OSP→야드 간 '이송 및 적재 시간차'를 반드시 계산식에 반영한다. Time-Lag는 상수가 아닐 수 있으므로(운반거리·물량 의존) 규칙을 명확히 정의하고 `docs/data_schema.md`에 기록한다.
- **⭐️ 고유 식별자를 Join Key로 활용**: 데이터에 존재하는 식별자(작업 일자, 로트 번호, 차량 번호, 구역 코드 등)를 Key로 사용하여 정확히 조인(Join)한다. 어떤 Key로 어떤 테이블을 조인했는지 문서화한다.
- **⭐️ 검증 게이트 (Validation Gate)**: 조인 **전에** 기본 무결성을 검사한다 — CaO 품위 범위(0~100%), 음수/0 물량 여부, 시간 역전(야드 시각 < 광산 시각), 식별자 중복·결측. 이상 발견 시 조용히 넘어가지 말고 사용자에게 보고한다.
- **산출물**: 통합 데이터셋 → `data/processed/`, 매칭 로직 → `src/matching/`

### `[ML-Engineer]` — 하이브리드 모델 (예측 + 최적화)
매칭된 통합 데이터셋을 바탕으로 **두 기능을 모두** 수행하는 하이브리드 모델을 구축한다.
1. **예측 (Prediction)**: 투입 데이터로 **야드 적재 완료 시 예상 CaO 평균 품위**를 사전 예측하는 회귀 모델. (기본: XGBoost/LightGBM 등 트리 기반, 교차검증·성능지표 필수)
2. **최적화 (Optimization)**: 목표치 **44.6% ±0.5**를 맞추기 위해 **OSP 각 구역에서 몇 톤(Ton)씩 가져와 배합**해야 하는지 역산·추천. (기본 접근: 예측/품위 모델 위에 **제약 최적화 솔버**(선형계획법 / `scipy.optimize`)를 얹어, 물량·재고·목표품위 제약을 만족하는 배합 비율을 산출)
- **⭐️ 성공 기준 (Acceptance Criteria)**: 목표(44.6%±0.5)가 의미 있으려면 예측 오차가 그보다 충분히 작아야 한다. **예측 MAE는 0.5% 미만(가급적 ≤0.2%)** 을 목표로 하고, 데이터의 시간 순서를 지키는 **시계열 교차검증(time-series CV)** 으로 검증한다. 성능지표(MAE·RMSE·R²)를 반드시 리포트한다.
- **⭐️ 달성 불가(Infeasibility) 처리**: OSP 재고 조합으로 목표 범위가 **물리적으로 불가능한 경우, 억지 답을 지어내지 않는다**(§2-1). 대신 ⓐ 가능한 범위에서 목표에 **가장 근접한 배합**을 제시하고, ⓑ "목표 미달 / 재고 제약으로 불가"임을 **명시적으로 경고**하며, ⓒ 어떤 제약이 병목인지 함께 보고한다.
- **산출물**: 모델 → `src/models/`·`src/optimization/`·`models/`, 실험 → `notebooks/03_modeling/`, `notebooks/04_optimization/`

### `[Research-Agent]` — 선행연구 조사
- 석회석 품위 관리, 시계열 데이터 매칭, ML 기반 배합 최적화 관련 최신 선행연구·기법 조사
- **산출물**: `docs/research/`

### `[Synthesis-Agent]` — 최종 리포트 작성
- 모든 분석 결과를 종합하여 **임원진 보고용 리포트 및 브리핑 문서** 작성 (시각화 포함)
- **출력 형식 (우선순위 전부 활용)**:
  - **Markdown** → `docs/reports/` (버전관리·재현성 기준본)
  - **HTML 아티팩트** → 차트 포함 인터랙티브 웹 리포트 (Artifact)
  - **PPTX 슬라이드** → 임원 브리핑 덱 (`pptx` 스킬)
  - **Notion 페이지** → 연결된 Notion 워크스페이스에 발행 (Notion MCP)
- 리포트는 Markdown을 기준본으로 먼저 작성하고, 사용자가 지정한 채널로 변환·발행한다.

---

## 4. 실행 워크플로우 (Pipeline)

**순차적 파이프라인** — 각 단계 완료 시 산출물을 검토하고 핸드오프 상태를 갱신한 뒤 다음으로 넘어간다.

```
[Data-Analyst]           [Matching-Agent]                    [ML-Engineer]
 데이터 구조 파악    ➡️   식별자 + Time-Lag 로직으로     ➡️    예측(CaO) +
 결측/EDA               통합 데이터셋 구축                  최적 배합 모델링
      │                        │                                  │
      └── data_schema.md ──────┴──── data/processed/ ─────────────┘
      (Research-Agent는 필요 시 병렬 지원, Synthesis-Agent는 최종 종합)
```

- **단계 전환 게이트**: 이전 단계의 데이터 품질·식별 기준이 확정되지 않으면 다음 단계로 넘어가지 않는다. 불확실하면 §2-2에 따라 질문한다.

---

## 5. 컨텍스트 관리 & 핸드오프 (Handoff Protocol) ⭐️

긴 대화로 인한 토큰 낭비와 컨텍스트 유실을 방지하기 위해 엄격히 준수한다.

### 5.1 상태 저장 (State Save)
아래 **트리거** 발생 시 반드시 `docs/handoff_state.md`를 생성/업데이트한다.
- 사용자가 **"handoff" / "핸드오프" / "저장해줘"** 라고 요청할 때
- 하나의 주요 단계가 끝날 때 (예: EDA 완료, 매칭 알고리즘 1차 완성, 모델 1차 학습 완료 등)
- **대화가 길어져 컨텍스트가 찰 때** — 요약/압축이 임박했다고 판단되면 사용자 지시가 없어도 선제적으로 저장한다. (토큰 낭비·컨텍스트 유실 방지)

> **⚠️ 저장 = 커밋까지**: 이 작업 환경은 세션 종료 시 초기화된다. `handoff_state.md`(및 `data_schema.md`)를 갱신하면 **반드시 git 커밋·푸시까지 완료**해야 다음 세션에서 읽을 수 있다. 갱신만 하고 커밋하지 않으면 상태가 유실된다.

### 5.2 `handoff_state.md` 필수 포함 내용
1. **완료 작업 요약** — 지금까지 성공한 데이터 전처리·매칭·모델링 내역
2. **핵심 데이터 스키마 & 주요 변수** — Join Key, Time-Lag 규칙, 단위, 주요 컬럼 정의
3. **현재 직면한 문제점 / 미해결 이슈**
4. **다음 행동 (Next Action)** — 새 세션에서 즉시 실행할 **구체적** 다음 행동과 **담당 Agent**

### 5.3 작업 재개 (Resume)
- 새 대화창 시작 시 → **§0 Boot Sequence** 를 따른다. (사용자 지시 없어도 `handoff_state.md` 먼저 읽고 복원)

---

## 6. 디렉토리 구조 (Repository Layout)

```
Blue365/
├── CLAUDE.md                  # 본 문서 (프로젝트 헌법)
├── README.md                  # 프로젝트 소개
├── requirements.txt           # Python 의존성
├── data/                      # 데이터 (실데이터는 .gitignore, 폴더구조만 커밋)
│   ├── raw/                   #   원본 엑셀 업로드 위치 (사용자 업로드처)
│   ├── interim/               #   전처리 중간 산출물
│   ├── processed/             #   매칭/조인 완료된 통합 데이터셋
│   └── external/              #   외부 참조 데이터
├── src/                       # 재사용 가능한 소스 코드 (파이프라인 단계별)
│   ├── data/                  #   로딩·전처리         [Data-Analyst]
│   ├── matching/              #   추적/조인 로직      [Matching-Agent]
│   ├── features/              #   피처 엔지니어링
│   ├── models/                #   예측 모델           [ML-Engineer]
│   ├── optimization/          #   배합 최적화 솔버    [ML-Engineer]
│   ├── visualization/         #   시각화 유틸
│   └── utils/                 #   공통 유틸
├── notebooks/                 # 탐색·실험 (실행 순서대로 번호 접두)
│   ├── 01_eda/                #   EDA                 [Data-Analyst]
│   ├── 02_matching/           #   매칭 실험           [Matching-Agent]
│   ├── 03_modeling/           #   예측 모델링         [ML-Engineer]
│   └── 04_optimization/       #   최적 배합 실험      [ML-Engineer]
├── docs/                      # 문서
│   ├── handoff_state.md       #   ⭐️ 세션 간 핸드오프 상태 (매 단계 갱신)
│   ├── data_schema.md         #   ⭐️ 데이터 스키마·Join Key·Time-Lag 규칙
│   ├── reports/               #   임원 보고 리포트    [Synthesis-Agent]
│   └── research/              #   선행연구 조사       [Research-Agent]
├── models/                    # 학습된 모델 아티팩트 (.pkl 등, .gitignore)
├── outputs/                   # 결과물
│   ├── figures/               #   생성된 차트/그림
│   └── predictions/           #   예측·추천 결과 파일
├── config/                    # 설정 파일 (paths·schema 등)
└── tests/                     # 테스트 코드
```

### 6.1 데이터 보관 정책 & 라우팅 (Local-Only Data Routing) ⭐️

**데이터는 로컬 전용이며 GitHub(원격)에 절대 커밋하지 않는다.** git에는 코드·문서·스키마만 올린다.

| 대상 | 로컬 경로 (기본) | 환경변수 오버라이드 | git 커밋 |
|------|------------------|---------------------|:--------:|
| 원본 데이터 | `data/raw/` | `BLUE365_DATA_DIR` | ❌ (`.gitignore`) |
| 중간/통합 데이터 | `data/interim/`, `data/processed/` | `BLUE365_DATA_DIR` | ❌ |
| 예측·추천 결과 | `outputs/predictions/` | `BLUE365_OUTPUTS_DIR` | ❌ |
| 학습된 모델 | `models/` | `BLUE365_MODELS_DIR` | ❌ |
| 그림/차트 | `outputs/figures/` | `BLUE365_OUTPUTS_DIR` | ❌ (선택) |
| **코드·문서·스키마·핸드오프** | 저장소 내 | — | ✅ |

- 경로는 항상 `config/paths.py`를 통해 참조한다(하드코딩 금지). 데이터를 저장소 **바깥** 로컬 폴더에 두려면 `BLUE365_DATA_DIR`를 지정하면 git 트리와 완전히 분리된다.
- **데이터 반입 방식**: 사용자가 로컬 `data/raw/`에 파일을 두거나, 원격 세션에서는 채팅에 첨부 → Main-Agent가 `data/raw/`에 배치. 어느 경우든 원격 커밋하지 않는다.
- **세션/머신 간 지속성**: 코드·문서·스키마·핸드오프는 git으로 이어지지만, **원본 데이터는 git에 없으므로 각 로컬 환경에 사용자가 직접 보유**해야 한다(원격 임시 세션은 종료 시 데이터 소멸).
- **커밋 전 안전 점검**: 항상 `git status`로 `data/`·`models/`·`outputs/` 실파일이 스테이징되지 않았는지 확인한다.

---

## 7. 기술 스택 & 관례 (Stack & Conventions)

- **언어/문서**: 문서·리포트·주석 설명은 **한국어**. 코드 식별자(변수·함수·컬럼)와 스키마 정의는 **영어** 유지.
- **핵심 스택**: Python 3.11 · pandas/numpy · openpyxl(엑셀) · scikit-learn · XGBoost/LightGBM · scipy(최적화) · matplotlib/seaborn/plotly
- **코드 스타일**: 주변 코드 관례를 따른다. 새 의존성 추가 시 `requirements.txt`에 반영.
- **데이터 무결성**: 파일을 덮어쓰기 전 항상 원본을 확인한다. `data/raw/`는 읽기 전용으로 취급하고, 가공물은 `interim/`·`processed/`에 저장한다.
- **데이터 보관**: **로컬 전용(§6.1)**. `data/`·`models/`·`outputs/predictions/` 실파일은 원격(git)에 커밋하지 않는다. 필요 시 `BLUE365_DATA_DIR` 등 환경변수로 저장소 바깥 로컬 경로 사용.
- **경로**: 하드코딩 대신 `config/paths.py`의 설정을 참조한다.

---

## 8. 커밋 & 브랜치

- 작업 브랜치: `claude/limestone-cao-tracking-system-15618s`
- 커밋 메시지는 명확하고 서술적으로 (한국어 가능). 주요 단계 완료 시 커밋.
- 사용자가 요청하기 전에는 PR을 만들지 않는다.
