# 데이터 스키마 & 매칭 규칙 (Data Schema & Matching Rules)

> `[Data-Analyst]` EDA 1차 결과. 원본: `data/raw/data_v1.xlsx` (8개 시트, 기간 2026-06-01 ~ **07-28**, 2026-07-28 갱신).
> ✅=데이터로 확인된 사실 / ❓=사용자 확인 필요(추측 금지, CLAUDE.md §2).

---

## 0. 야드변경 시트 (2026-07-24 추가) ⭐️
`기존라인 야드변경`(9행)·`신설라인 야드변경`(22행) — 컬럼: `변경일·변경시간·Yard·석회석CaO·석회석MgO·야드물량`.
- 각 행 = 야드 변경 이벤트(그 시점부터 해당 야드에 실제 적재한 물량·품위). 야드: 기존(Y1/Y2), 신설(Y1/Y2-내수).
- 용도: **야드변경 기반 추적 Sankey(라인→야드, 링크=야드물량, CaO/MgO 토글)** + 변경일자별 품위 추이. (누적 인출물량 대체 뷰)
- 정제: `clean.clean_yard_change`, 로더: `dataset.load_yard_change`.

## 1. 데이터 소스 개요 (원 6개 시트 + 야드변경 2개 + OSP 재고 1개 = **9개**)

> 📅 **2026-08-05 갱신본 기준 행수·기간** (아래 표는 최초 작성 시점 값이라 낡았다 — 최신은 이 블록):
> 49Q 223행(06-10~08-03) · 47Q 90행(06-11~08-03) · OSP인출 기존 103행(06-10~08-02) /
> 신설 255행(06-01~08-03) · CNA 28,907행(06-10~**08-05**) · 45Q 3,839행(06-11~07-21, **불변**) ·
> 야드변경 기존 9건(~07-15, 불변) / 신설 28건(~08-04) · OSP 재고 2,557행(2024-04-09~08-05).


| 공정 | 시트명 | 행수 | 성격 | 기간 |
|------|--------|------|------|------|
| ① 광산 | `광산(49Q)_XRF` | 153 | 채굴 원위치 품위 (XRF), 교대(1·2·3차)/일 | 06-10~07-22 |
| ① 광산 | `광산(47Q)_감마레이` | 71 | 채굴 원위치 품위 (감마레이), 시작~종료시각 | 06-11~07-21 |
| ② OSP | `OSP인출_기존` | 91 | 기존라인 인출 이벤트 (시각별) | 06-10~07-11 |
| ② OSP | `OSP인출_신설` | 150 | 신설라인 인출 이벤트 (시각별) | 06-01~07-14 |
| ③ 야드 | `CNA data` | 20,952 | CNA 실시간 분석기 (약 2분 간격) | 06-10~07-21 |
| ❓ | `45Q 감마레이 data` | 3,839 | 연속측정(야드형 구조) — 정체 확인 필요 | 06-11~07-21 |

## 2. 핵심 컬럼

### ① 광산 (Mine)
- `채굴일자`(date), `채굴시간(교대)`[1·2·3차] 또는 `시작시간`/`종료시간`
- `공정구분` = **기존 / 신설 / 운휴** (라인 구분) ✅
- `OSP적재구역` = 채굴물을 적재한 품위 구역코드 ✅ (복합코드 존재: "50/55", "55-45", "100-0" 등 → 한 교대에 여러 구역 적재)
- `CaO품위`, `MgO품위`(%), `이송물량(톤)` (49Q는 object형+'-' 혼입 → 정제 필요)

### ② OSP (인출)
- `일자`(date), `인출시간`(time)
- `공정구분`: 기존시트=전부 '기존' ✅ / 신설시트=전부 NaN(시트명으로 '신설' 보정 필요)
- `P/W1호~P/W4호` = **인출 품위 구역코드**로 추정(값 20~95, 광산 OSP적재구역과 동일 어휘) ❓
  - 기존라인 → P/W1·2호 사용 / 신설라인 → P/W3·4호 사용 ✅ (구조적 분리 확인)
- `인출량`(톤) = 그 인출 회차 총량 ✅ (구역별 배분 규칙 ❓)

### ③ 야드 (Yard)
- `일자`, `시간`, `CaO`, `MgO`(%), `적재물량` (CNA=TPH / 45Q=B/S, 단위 상이 ❓)
- `Yard` 컬럼: 두 시트 모두 전부 결측 → 미사용/플레이스홀더

## 3. Join Key (식별자) — ✅ 확정 (사용자 확인)

| 축 | Key | 규칙 |
|----|-----|------|
| 라인 | `공정구분` (기존/신설) | 기존→P/W1·2호, 신설→P/W3·4호 |
| 시간 | 일자 + 시각 → datetime | 시간창(1h) 집계 후 Time-Lag 정렬 |
| 공간(품위) | `OSP적재구역` ↔ `P/W호` (구역 20~95) | 광산 구역-품위 평균 → OSP 인출에 예상 CaO 부여 |
| 배분 | 인출량 ÷ 구역수 (균등) | 사용자 확정 |
| 최종 목표 | `CNA data` | 야드 적재 품위 기준 (45Q/47Q/49Q = 채굴구역 3곳) |

## 4. Time-Lag 규칙 — 🔬 데이터 추정 (1차)

- 방법: OSP 시간별 예상CaO를 지연시키며 야드 CaO와 피어슨 상관 최대 지연 탐색 (`estimate_time_lag`).
- **1차 결과: 최적 지연 ≈ 11시간, 그러나 상관 0.17로 약함.** → §7 참조. 매칭 신호 강화 필요.

## 6.5 도메인 확정 (2026-07-23, 사용자) ⭐️ 중요 정정
- **기존 ≠ 신설 = 서로 다른 야드**: 기존 인출 → **4-5K 킬른** 야드 / 신설 인출 → **6-7K 킬른** 야드. → 절대 합치지 않고 라인별 분리 모델링.
- **P/W1~4호 = 인출 지점(위치)**, 품위 등급 아님. 지점당 50% 균등 인출. (구역숫자 20~95는 위치ID)
  - 검증: 구역숫자 vs CaO 상관 = -0.001, 구역 내 CaO std 2.58 → 숫자는 품위 무관 확인.
- **품위 매칭은 시간인지로**: 지점의 품위는 '그 지점에 최근 적재된 광산 품위'(merge_asof)로 부여.
- **야드 페어링(✅ 사용자 확정)**: 기존↔`45Q 감마레이 data`(4-5K 킬른), 신설↔`CNA data`(6-7K 킬른).
  - 초기에 데이터 상관만 보고 반대로 추정했으나(기존↔CNA), **사용자 도메인 확정이 우선**하여 정정됨.
  - **정본은 `config/schema.py`의 `YARD_PAIR` 하나뿐**이다. 스크립트·노트북에서 야드 시트를 직접 지정하지 말 것
    (`scripts/build_matched_dataset.py`가 옛 페어링을 하드코딩해 잘못된 매칭셋을 만든 버그가 실제로 있었음 — 2026-07-28 수정,
    회귀 테스트 `tests/test_edge_cases.py::test_scripts_do_not_redefine_yard_pairing`로 고정).

### 6-0. 채굴 교대 시간대 & 광산 대표 시각 ⭐️ (2026-07-28 사용자 확정)

광산 데이터에는 원래 시각이 없어 일 단위로만 배치했고, 구간 경계에서 그 날 채굴분이
통째로 빠지거나 하루 전체가 들어가는 문제가 있었다(실측 평균 27.9%, 최대 100% 차이).
사용자 확정 교대 시간대로 **대표 시각**을 부여해 해소했다.

| 교대 | 시간대 | 대표 시각(중점) | 49Q 건수 |
|------|--------|:---------------:|:--------:|
| 1차 | 08:00~16:00 | **12:00** | 61 |
| 2차 | 16:00~24:00 | **20:00** | 61 |
| 3차 | 00:00~08:00 | **04:00** | 61 |

- **대표 시각 = 교대 중점**. 시작 시각을 쓰면 경계에서 최대 8시간을 앞당기게 되므로 중점이 오차가 가장 작다.
- **47Q는 실측 시작·종료 시각**이 있으므로 그 **중점**을 쓴다(구간 평균 3.65시간). 종료<시작이면 자정 넘김으로 보고 하루를 더한다.
- 정의는 `config/schema.py`의 `SHIFT_HOURS`, 구현은 `clean.shift_midpoint()`·`clean._time_midpoint()`.
- **인수인계 30분은 반영하지 않는다(`HANDOVER_APPLIED=False`)** — 대표 시각이 15분 움직일 뿐이고,
  33개 야드변경 구간 **전부에서 귀속이 바뀌는 행이 0건**이었다(최소 구간 40시간 대비 0.62%).
  필요해지면 `HANDOVER_APPLIED=True` 한 줄로 켤 수 있다.
- **총량은 보존된다**: 시각 부여 후에도 결측 0건, 라인·소스별 합계가 원본과 100% 일치.

### 6-0-2. OSP 실사 재고 시트 ⭐️ (2026-07-30 추가)

시트명 `OSP 재고`. **좌우 2블록**이 나란히 있고 가운데(4번 열)는 빈 칸이다.

| 블록 | 열 | 라인 | 행수 | 기간 |
|------|----|------|-----:|------|
| OSP1 | 0~3 | **기존** | 2,525 | 2024/04/09 ~ 2026/07/30 |
| OSP2 | 5~8 | **신설** | 2,515 | 동일 |

- 헤더는 **3번째 행**(index 2): `날짜 · 차수 · OSP 구분 · 재고량`. 데이터는 4번째 행부터.
- **차수 = 채굴 교대**(1차 08~16 · 2차 16~24 · 3차 00~08)이고, **재고 실사는 교대 중간에 1시간 동안** 이뤄진다.
  대표 시각은 그 파악 시간대의 중점을 쓴다: **1차 12:30 · 2차 20:30 · 3차 04:30** (`STOCK_TAKE_HOURS`).
  → 채굴 대표 시각(12:00/20:00/04:00, §6-0)과 **다르다.** 혼동 금지.
- 하루 안 기록 순서는 `3차 → 1차 → 2차`이며, 대표 시각으로 정렬하면 시간순과 일치한다.
- **실사값은 1,000톤 단위 개략치**다(전 구간 100%가 1,000의 배수, 고유값 27개). 절대 수준·추세 파악에는 쓰되 정밀 대조에는 부적합.
- **중복**: 같은 (라인·날짜·차수)가 두 번 기록된 경우가 전 기간 14건. **마지막 기록을 채택**하고 검증 게이트가 건수를 보고한다. *(모두 2026-06 이전이라 현재 분석 기간에는 영향 없음)*
- **결측**: 최근 실사 미입력 2건(2026-07-30 1차). 결측으로 유지한다.
- 구현: `clean.clean_osp_stock()`, `dataset.load_osp_stock()`, 대조 `dataset.stock_vs_flow()`.

#### ⚠️ 미해결: 실사 재고와 흐름 계산의 큰 격차

| 라인 | 실사 변화 | 흐름 계산(적재−인출) | 격차 | 일평균 |
|------|----------:|--------------------:|-----:|-------:|
| 기존 | -6,000톤 | -20,573톤 | **+14,573톤** | +370톤/일 |
| 신설 | +4,000톤 | -72,344톤 | **+76,344톤** | +1,624톤/일 |

실사 재고는 정상 범위(기존 28~45천톤, 신설 22~40천톤)를 유지하는데, 광산 적재−인출로 계산하면
**신설은 재고가 마이너스**가 되어야 한다(물리적으로 불가능). 즉 **광산(49Q·47Q) 기록에 잡히지 않는
유입이 있거나, 인출 기록의 기준이 다르다.** 원인 미확정이므로 **어느 한쪽을 맞다고 단정하지 않고
둘 다 표시**한다(§2-1). → 사용자 확인 대기 항목.

### 6-0-3. 인출 계량 = 벨트스케일 & 지시 배율 β ⭐️ (2026-07-30 사용자 확인)

**OSP 인출량은 벨트스케일(belt scale)로 계량한다.** 벨트스케일 오차는 통과 물량에
**비례**하므로, 실사 재고와의 격차를 배율 **β = 실제 ÷ 계량기 지시값** 으로 표현하는 것이
물리적으로 타당하다(가산 보정보다 이 형태가 실제로 잔차도 작았다).

| 라인 | 전체 β | 95% 신뢰구간 | 해석 |
|------|------:|--------------|------|
| 기존 | **0.927** | 0.921 ~ 0.933 | 계량기가 약 **7.9% 과대 지시** |
| 신설 | **0.906** | 0.903 ~ 0.910 | 계량기가 약 **10.4% 과대 지시** |

**10일 단위 β 추이** (오차막대 = 95% CI, 겹치지 않으면 실제 변화):

| 라인 | 구간별 β | 판정 |
|------|----------|------|
| 기존 | 0.855 → 0.968 → 0.998 → **1.040** | **한 방향으로 상승**(10일당 +0.058) — 지시값이 점점 변함, **교정 점검 대상** |
| 신설 | 0.882 → 0.810 → 0.973 → 0.824 → 0.888 | 추세 없이 **들쭉날쭉**(0.81~0.97) — 그때그때 조건에 따라 흔들림 |

- 두 라인 모두 인접 구간의 신뢰구간이 겹치지 않아 **노이즈가 아닌 실제 변동**이다.
- 구현: `dataset.calibrate_flow()`(전체 β·표준오차), `dataset.calibration_windows()`(구간별 β),
  `figures.calibration_drift()`(추이 차트).
- ⚠️ 실사 재고도 육안이라 오차가 있으므로, β 는 **'계량기만의 오차'가 아니라 두 기록 간 상대 배율**이다.
  다만 실사 오차는 무작위(평균 0)에 가깝고 β 는 체계적이라, 계량 쪽 요인이 지배적일 가능성이 높다.

### 6-0-4. OSP 적재구역 코드 표기 ⭐️ (2026-08-05 갱신)

한 회차에 여러 구역을 적재하면 코드가 복합 문자열로 들어온다. **구분자를 놓치면 구역이
조용히 사라지므로**(숫자 변환 실패 → 토큰 폐기) 관측된 표기를 모두 정본에 반영한다.

- 구분자: `/` · `-` · `~` · **`,`(2026-08-05 갱신본에서 처음 등장)** · 공백
  → `config.schema.ZONE_DELIMITERS = r"[/\-~,\s]+"`
- 실제 관측 예: `50/55` · `55-45` · `100-0` · `15~35` · `95-85-80` · `65/40/70/20-80` ·
  `55/75/50/75/40~45/75` · **`25~30, 65~40`** · **`35~60, 75~85`** · `60~` · `~70`
- `운휴`(가동 안 함)와 빈칸은 구역 없음(`[]`)으로 처리하되, **물량이 있으면 행 자체는 살린다**
  (`zone=NaN`) — 구역만 모르는 것이지 물량이 없는 게 아니다.
- 회귀 테스트: `tests/test_edge_cases.py::test_zone_codes_split_on_comma`

### 6-0-5. β 를 식별할 수 없는 구간 ⭐️ (2026-08-05 확립)

라인이 멈춘 구간에서는 인출이 거의 없는데 재고는 움직여, 배율 β 의 분모가 0에 가까워지고
값이 몇 배로 튄다(실제로 기존 라인 07/21~07/31 창이 **β=6.98**로 나왔다).

- **판정 기준**: 창의 인출 최대 누적이 `MIN_WINDOW_TON = 5,000톤` 미만이거나,
  β 가 타당범위 `PLAUSIBLE = (0.7, 1.3)` 밖이면 → `ok=False`
- **처리**: 그래프·추세 판정에서 제외하되 **몇 개를 왜 뺐는지 리포트 본문에 명시**한다(§2-1).
  조용히 지우지 않는다.
- 정의 위치: `src/models/dataset.py` (`calibration_windows`)
- 회귀 테스트: `tests/test_edge_cases.py::test_calibration_window_rejects_unidentifiable_beta`

### 6-0-6. 기존 라인 미가동 구간 (2026-07/21~07/28) ⭐️ 사용자 확정 (2026-08-05)

- **기존 라인은 가동하지 않았다.** 채굴분은 **OSP 재고를 채우는 용도**로 적재만 했고 **인출은 없다.**
- `OSP인출_기존`의 07/20→07/29 공백은 **기록 누락이 아니라 실제 미가동**이다 → 인출 0 이 참값.
- 결과: 이 구간은 **인출·벨트스케일 오차가 개입하지 않는 깨끗한 검증창**이다.
  `Δ재고 = 적재` 항등식이 그대로 성립하므로 **광산 적재 기록을 직접 감사**할 수 있다.
- 이 구간에서 β(계량 배율)는 **분모가 0이라 원리적으로 식별 불가** → §6-0-5 가드가 자동 제외한다.
- 감사 결과(07/21 04:30 → 07/27 20:30, 실사 Δ**+10,000톤**):

  | 47Q 버전 | 기존 적재 | 신설 적재−인출 | 기존 불일치 | 신설 불일치 |
  |---|---:|---:|---:|---:|
  | 갱신 전(07-30본) | 18,966 | +5,990 | **+8,966** | −3,010 |
  | 갱신 후(08-05본) | 26,738 | **−1,844** | **+16,738** | −10,844 |

  → **총 물량은 두 버전이 동일**(160,957 vs 160,893톤)하고 총 불일치도 같다(+5,957 / +5,893).
  차이는 오직 **라인 배분**이며, 갱신 후는 ⓐ 기존 불일치를 2배로 키우고
  ⓑ 신설을 **적재<인출(−1,844)로 만들어 재고가 +9,000톤 늘어난 실사와 모순**시킨다.
- **용량 제약**: OSP1 재고는 이 구간 32,000→최대 44,000톤, 역대 최대 45,000톤.
  즉 담을 수 있는 여유가 **약 13,000톤**뿐인데 갱신 후 기록은 26,738톤을 적재했다고 한다 → **물리적으로 불가능**.
- **결론**: 47Q 재갱신의 **형식·시간해상도·품위정밀도는 개선**이지만, **`공정구분`(라인 귀속)은 실사 재고와 배치**된다. 현장 재확인 대상.

### 6-0-7. 복합 라인 라벨 & 라인 귀속 정정 ⭐️ (2026-08-05)

**① 복합 `공정구분` 라벨 (버그 수정)**
광산 시트의 `공정구분` 에 한 교대가 두 상태에 걸친 값이 들어온다. 이전에는 라인명과
달라 **라인별 집계에서 통째로 빠졌다 — 7행 21,065톤(광산 총량의 1.7%).**

| 라벨 | 물량 | 가동 시간 | 근거 |
|---|---|---|---|
| `운휴/기존` · `운휴/신설` | 뒤쪽 실제 라인에 **전량 귀속** | **교대의 50%** (`IDLE_ACTIVE_RATIO`) | ✅ 사용자 확정 — "절반은 운휴, 절반만 이송" |
| `기존/신설` | 두 라인에 **균등 배분** | 100% | ✅ 사용자 확정 — 구역 `45/70` 이어도 위치 대응이 아니라 균등 배분 |
| `운휴` 단독 | 행 제외 | 0% | 실물량 없음 |

> ⭐️ `운휴/X` 의 **물량은 전량 X 라인 것**이고 바뀌는 것은 **가동 시간**뿐이다.
> 정제 결과에 `active_ratio` 컬럼으로 실려 나가며, 생산능력(C/R) 상한과 비교할 때 쓴다.
> 실제로 복합 라벨 6행을 4시간 가동으로 환산하면 473~1,191 t/h 로 모두 능력 상한 안에 들어온다
> (8시간으로 보면 236~596 t/h 로 비정상적으로 낮다) — 해석의 정합성이 확인된다.

- 정의: `config.schema.LINE_SPLIT_DELIM` · 구현: `clean.split_line_label`
- **검증 게이트에 `line_unknown` 검사 추가** — 알 수 없는 라벨이 남으면 물량과 함께
  ERROR 로 보고한다. 다시는 조용히 빠지지 않는다.
- ⚠️ `기존/신설` 의 균등 배분은 **가정**이다. 구역과 라인이 위치로 대응한다면
  (예: 구역 `45/70` → 45=기존, 70=신설) 배분이 달라진다 — 현장 확인 대상(해당 1행 5,762톤).
- 회귀 테스트: `test_composite_line_labels_are_split_not_dropped`, `test_validation_flags_unknown_line_label`

**② 47Q 라인 귀속 정정 (사용자 확정)**
§6-0-6 감사 결과와 사용자 확정("갱신 전 기준이 맞다")에 따라, 08-05 갱신본이
신설→기존으로 옮긴 블록을 되돌린다.

- 방법: 갱신 전 기록에 **시간 겹침 다수결**로 대응 → 4개 블록만 바뀐다(추측 없음).
- 정의: `config.schema.MINE_LINE_OVERRIDES` — **시각·라인 라벨만** 담는다(품위·물량은 원본 그대로).
- 적용: `clean.apply_line_overrides` (47Q 전용). 규칙이 원본과 안 맞으면 **경고**한다.
- 결과: 07/22~07/27 기존 라인 47Q 15,007 → **7,797톤** (갱신 전 원본 7,173톤과 624톤 차 —
  블록 경계가 달라 생기는 잔차이며, 그 세분화 자체는 갱신본의 개선점이다).
- 재고 수지 개선: 기존 불일치 +16,738 → **+9,527톤**, 신설 −10,844 → **−3,634톤**,
  신설이 다시 적재>인출이 되어 **재고 증가 방향과 부합**(모순 해소).
- ⚠️ **원본 시트의 `공정구분` 이 현장에서 바로잡히면 이 표를 지워야 한다.**

### 6-0-8. 생산방법 (C/R) & 생산능력 ⭐️ (2026-08-05 · **v2 반영 완료**)

`data_v2.xlsx` 의 49Q 시트에 **`C/R(생산방법)`** 컬럼이 추가되었다. 값과 시간당 생산능력:

| C/R | 설비 | 시간당 생산능력 |
|---|---|---|
| `G/C` | 자이레토리 크러셔 (Gyratory Crusher) | **1,500 ~ 1,600 t/h** |
| `H/C` | 함마 크러셔 (Hammer Crusher) | **1,000 ~ 1,100 t/h** |
| `G/C+H/C` | 두 크러셔 **동시 가동** | **1,500 ~ 1,600 t/h** |

> ⭐️ **동시 가동이 가산되지 않는다** — 1,600 + 1,100 ≠ 2,700 이고 G/C 단독과 같다.
> 크러셔 **하류에 병목**(컨베이어·이송 계통)이 있다는 뜻이며, 동시 가동은 처리량이 아니라
> **여유·이중화**를 얻는 운전이다. 배합 최적화의 생산 제약식도 이 형태로 세워야 한다
> (`min(설비합, 하류상한)`, 단순 합산 금지).

정의: `config.schema.CRUSHER_CAPACITY_TPH` · `COL_CRUSHER` · 테스트 `test_crusher_capacity_spec_is_not_additive`

**능력 상한 감사 결과 (v2 실측) — ✅ 초과 0건**

`dataset.capacity_audit()` 이 교대별 `물량 ÷ (8h × active_ratio)` 를 C/R 상한과 대조한다.
검증 게이트에 연결되어 있어 상한 초과가 나오면 **ERROR 로 차단·보고**된다.

| C/R | 교대 수 | 중앙 t/h | 최대 t/h | 상한 | 초과 |
|---|---:|---:|---:|---:|---:|
| G/C | 84 | 880 | **1,588** | 1,600 | **0** |
| G/C+H/C | 35 | 889 | 1,341 | 1,600 | **0** |
| H/C | 16 | 661 | 800 | 1,100 | **0** |

→ **주어진 능력치가 실제 조업 기록과 완전히 정합**한다(G/C 최대 1,588 이 상한 1,600 에 근접하되 넘지 않음).
   동시 가동 최대(1,341)가 G/C 단독 최대(1,588)보다 **낮다** — 가산되지 않는다는 설명과 부합.

C/R 값 중 `운휴`·`0`·빈칸(총 53행)은 **전부 이송물량 결측**이라 생산이 없는 행이다(`S.CRUSHER_IDLE`).

### 6-0-9. 수항 (Surge Bin / 사일로) ⭐️ (2026-08-05 사용자 확정 · v2 비고 반영)

**광산 → OSP 경로는 둘이다.**

```
광산 채굴 ─┬─→ [수항: 사일로 10,000톤, 적재 ~1,500 t/h] ─→ G/C ─┐
           └──────────────────────────────→ H/C ───────────────┴─→ (OSP행 벨트 1개) ─→ OSP
```

- 동시 가동(G/C+H/C)이라도 **OSP 로 가는 벨트는 1개**라 거기서 만나 적치된다.
  → **`G/C+H/C` 능력이 가산되지 않는 물리적 이유**가 바로 이것이다(§6-0-8).
- ⭐️ **수항은 버퍼다.** 채굴 시점 품위와 OSP 적재 품위 사이에 **지연·혼합**이 생긴다 →
  상류(광산)–하류(야드) 상관이 0.21~0.25 로 낮은 것의 **물리적 설명 후보**.
- 정의: `config.schema.SURGE_BIN_CAPACITY_TON` (10,000) · `SURGE_BIN_FILL_TPH` (1,500)

**⭐️ 직전 갱신의 '품위 삭제 18칸'은 삭제가 아니라 수항 이동이었다**

08-05 갱신 때 49Q 품위 18칸이 빈칸이 된 것을 기록 삭제로 보고했으나, v2 의 비고를 보니
**그 값들이 `(수항채움 CaO, MgO, 톤)` 형태로 옮겨져 있었다.** 5개 행에서 값이 정확히 일치한다.

| 행 | 일자 | 구본 CaO/MgO | v2 비고 |
|---|---|---|---|
| 147 | 07/22 | 43.11 / 3.39 | `(수항채움 43.11, 3.39, 2400톤)` |
| 153 | 07/23 | 47.29 / 1.07 | `(수항채움 47.29, 1.07, 3040톤)` |
| 159 | 07/24 | 47.08 / 2.37 | `(수항채움 47.08, 2.37, 3600톤)` |
| 165 | 07/25 | 45.63 / 3.00 | `(수항채움 45.63, 3.00, 2,640톤)` |
| 166 | 07/25 | 38.57 / 0.58 | `(수항채움 38.57, 0.58, 1,520톤)` |

→ 그 물량은 **OSP 로 간 것이 아니라 수항에 들어간 것**이므로 이송물량·품위를 비운 것이 맞다.
   **기록 정정이 아니라 경로 구분**이었다. (§6-0-6 의 '삭제 18칸' 우려는 해소)

**비고에서 뽑는 것** (`clean.parse_surge_fill` · `parse_note_events` · `dataset.load_surge_fills`)

| 패턴 | 의미 | 건수 |
|---|---|---:|
| `(수항채움 CaO, MgO, 톤)` | 수항 적재 — 품위까지 | 11 |
| `수항채움 ( 덤프기준 N톤 )` | 수항 적재 — 톤만 | 4 |
| `수항재고부족` | 수항 고갈로 생산 중단 | 3 |
| `수항 단독` | 수항 재고만으로 생산 | 2 |
| `OSP 만실` / `야드 만실` | 적재 상한 도달 | 5 / 5 |

수항 적재 누적 **26,960톤** (07/15~07/25), 단일 최대 3,600톤 — 용량 10,000톤 내에서 정합.

⚠️ **수항채움 물량은 그 시점의 OSP 적재가 아니다.** 나중에 G/C 를 거쳐 나가므로 재고 수지에서
   분리해 다뤄야 한다. `load_surge_fills()` 가 별도 프레임으로 제공한다.

### 6-0-10. 비고에서 뽑는 실제 생산 구간 & 구역 창 ⭐️ (2026-08-05 · v2)

**① 수항채움 행의 `이송물량(톤)`은 OSP 몫만이다** (사용자 확정)
→ 그 교대의 총 채굴량 = `이송물량`(OSP행) **+** `수항채움`(사일로행). 두 경로는 서로 배타적이므로
   OSP 재고 수지는 지금처럼 `이송물량`만 쓰면 맞다. 수항채움은 별도 프레임(`load_surge_fills`)으로 관리한다.

**② 수항 인출 기록은 없다** (사용자 확정)
→ 수항 재고 곡선을 직접 그릴 수 없다. G/C 생산분이 곧 수항 인출이므로
   `누적 채움 − 누적 G/C 생산` 으로 **역산**해야 하며, `G/C+H/C` 동시가동 교대는 두 설비의 몫을
   나눌 수 없어 **구간 추정(하한~상한)** 으로만 낼 수 있다. (미구현 — 다음 단계)

**③ 붙여넣기 의심 수항채움 자동 검출**
서로 다른 날짜에 `(CaO, MgO, 톤)`이 완전히 같으면 붙여넣기 오류로 본다
(사용자 판단: 같은 품위를 두 번 가질 수 없다). 어느 쪽이 원본인지 단정할 수 없으므로
**지우지 않고 `suspect=True` 로 표시**만 하고 품위 통계에서 제외한다(§2-1).
현재 검출: **07/20 3차 · 07/25 3차 의 `(49.25, 0.79, 1200톤)` 2건.**

**④ 실제 생산 구간 파싱** (`clean.parse_note_window`)

비고에는 **교대 전체 생산구간**과 **구역별 부분 적치구간**이 *같은 표기*로 섞여 있다.

| 비고 | 실제 의미 |
|---|---|
| `00:20~07:50분 생산종료` | 교대 전체 생산구간 (7.5h) |
| `06:30-07:05 까지 40번 적치` | 구역 40번 **부분** 적치구간 (0.58h) |

구분할 표지가 없으므로 **물리적 가능성**으로 가른다 — 채택 조건 2가지:
1. 구간 길이 ≤ 교대 길이(8h)
2. `물량 ÷ 구간시간` ≤ 해당 C/R 생산능력 상한

구간은 적힌 **모든 시각의 최소~최대**로 잡는다. 중간에 쉬는 시간이 있으면 실제 가동은 더 짧으므로
여기서 나오는 t/h 는 **하한**이다(과대평가하지 않는 쪽).
`24시`는 자정(1440분)으로 처리한다 — 0 으로 접으면 `19:50~24:00` 이 19.8시간으로 벌어진다.

**결과**: 물량 있는 교대 중 **20개**에서 실제 생산구간 확보.

| | 8h 가정 | 실제 구간 |
|---|---:|---:|
| 가동시간 중앙 | 8.00h | **5.11h** (8h의 **64%**) |
| t/h 중앙 | 837 | **1,480** |

→ ⭐️ **8시간 가정은 시간당 생산량을 크게 과소평가한다.** 실제로는 **능력 상한(1,600) 근처**에서
   돌다가 나머지 시간은 멈춰 있는 운전 패턴이다. `capacity_audit()` 이 `op_hours`·`op_tph` 로 함께 보고한다.
   *(감사의 초과 판정 자체는 계속 8h 기준을 쓴다 — 구간 채택이 능력 상한을 조건으로 하므로,
   그 시간으로 다시 판정하면 순환논리가 된다.)*

**⑤ 구역 창** (`clean.parse_zone_windows`) — `16:20~18:30분 35번` 형태

구역 단위 실제 적치 시각이라 교대 중점보다 훨씬 정밀하지만 **표기가 드물다(3건)**:
`06/18 3차 구역25 00:20~04:00` · `07/07 2차 구역35 16:20~18:30` · `07/07 2차 구역60 18:30~20:40`.
표본이 적어 지금은 Time-Lag 추정에 반영하지 않고 파서만 마련해 둔다.

### 6-0-11. 수항 재고 곡선 — 어디까지 알 수 있나 ⭐️ (2026-08-05)

**연속 재고 곡선은 만들 수 없다.** 지어내지 않고 그 사실과 근거를 낸다(§2-1).

| 항목 | 톤 |
|---|---:|
| 유입 기록 (비고 `수항채움`, 15건) | **26,960** |
| 유출 하한 (G/C 단독 교대 이송량) | **622,349** |
| 유출 상한 (+ G/C+H/C 동시가동) | 851,794 |
| H/C 단독 (수항 미경유) | 76,620 |

→ `재고 = 유입 − 유출` 이면 **−595,389톤**. 유출이 유입 기록의 **23배**다.

**원인 규명**: 수항채움 15건을 그 교대의 운전 상태와 대조하면 —

| 상황 | 건수 |
|---|---:|
| 생산정지 — 수항만 채움 (이송물량 없음) | 8 |
| H/C 로 생산하며 수항 비축 | 5 |
| G/C 가동 중 채움 (이례) | 1 |

→ **`수항채움`은 "G/C 를 안 쓰는 동안 비축한 양"만 적은 것**이다. 정상 운전 중에는
   광산→수항→G/C 가 연속으로 흐르므로 따로 적을 이유가 없어 기록되지 않는다.
   매 교대의 수항 유입이 없으므로 연속 곡선은 원리적으로 불가능하다.

**대신 가능한 것 — '고갈' 앵커 기반 국소 수지** (`dataset.surge_cycles`)

`수항재고부족` 이벤트는 **그 시점 재고 ≈ 0** 이라는 앵커다. 직전 고갈 이후 채운 양이
다음 고갈까지 전부 빠져나간 것으로 보면 국소 수지가 닫힌다.

| 사이클 | 채움 | 시간 | 평균 인출률 |
|---|---:|---:|---:|
| 07/15 12시 → 20시 | 4,560 | 8.0h | 570 t/h |
| 07/20 04시 → 07/22 20시 | **10,400** | 64.0h | 162 t/h |
| 07/23 12시 → 20시 | 3,040 | 8.0h | 380 t/h |
| 07/24 12시 → 07/25 04시 | 4,800 | 16.0h | 300 t/h |
| 07/25 12시 → 07/27 20시 | 4,160 | 56.0h | 74 t/h |

- **5개 사이클 전부 물리적으로 성립**(인출률 74~570 t/h, G/C 상한 1,600 이내).
- ⭐️ **누적 채움 최대 10,400톤** — 사용자 확인 용량 **10,000톤**과 거의 일치(+4%).
  독립적인 두 정보가 맞아떨어져 파싱·해석이 옳다는 강한 방증이다.
- 차트 `figures.surge_timeline()` — 그리는 값은 **실제 재고의 하한**임을 캡션에 명시한다.

**⚠️ 파싱 버그 (수정)**: `수항재고부족` / `수항재고 부족` 처럼 **띄어쓰기만 다른 표기**를
놓쳐 고갈 이벤트 10건 중 **6건이 빠져** 있었다. 공백을 지우고 대조하도록 고쳤다
(`clean.parse_note_events`, `dataset.surge_events`). 회귀 테스트 `test_surge_events_catch_whitespace_variants`.

**혼합(mixing) 가설 — 데이터가 지지하지 않음**

수항(1만톤)을 완전혼합조로 보면 출구 품위는 입구의 지수가중이동평균이고
시정수 τ = 재고÷처리율 ≈ 10,000/1,500 ≈ **6.7h** 이다. τ 를 0~24h 로 훑어
상류→야드 최대상관을 측정한 결과:

| τ | 기존 | 신설 |
|---|---:|---:|
| 0 (혼합 없음) | +0.300 | **+0.338** |
| **6.7 (물리적 예상)** | +0.292 | +0.315 |
| 24 | **+0.347** | +0.289 |

→ **물리적으로 예상되는 τ=6.7h 에서는 두 라인 모두 상관이 오히려 떨어진다.**
   기존 라인이 τ=24h 에서 +0.047 오르지만 그때 lag 가 0h 가 되어 공정 지연과 배치되므로,
   물리적 혼합이 아니라 **단순 평활 효과**로 본다.
   → 현재 데이터로는 **"수항 혼합이 상류-하류 괴리의 주원인"이라고 말할 수 없다.**
     수항 유입 기록이 없어 실제 체류시간을 알 수 없는 것이 한계다.

### 6-1. 결측(빈칸) 품위 처리 규칙 ⭐️ (2026-07-28 확립)

원본에 **품위가 빈칸인 행**이 존재한다(물량은 있는데 분석값이 없는 경우).

| 소스 | CaO 결측 | MgO 결측 | 비고 |
|------|:--------:|:--------:|------|
| 광산 49Q(XRF) | 1행 (0.5%) | 1행 (0.5%) | 영향 미미 |
| **광산 47Q(감마)** | **12행 (15.0%)** | **12행 (15.0%)** | **물량의 13.6%** |
| OSP 인출 | 0% | — | |
| 야드 CNA(신설) | 0% | 0% | |
| **야드 45Q(기존)** | 0% | **521행 (13.6%)** | MgO만 결측 |
| 야드변경 | 0% | 0% | |

- **규칙**: 품위 빈칸 행은 **물량(합계)에는 포함**하되, **평균 계산의 분자·분모 양쪽에서 제외**한다.
  빈칸을 0으로 넣으면 평균이 0쪽으로 끌려 내려간다(실제로 47Q CaO가 45.61 → 36.63으로 왜곡됐던 버그).
- **CaO와 MgO는 결측 위치가 다르므로 성분별로 유효물량·유효건수를 따로 집계**한다(45Q는 MgO만 결측).
- 구현: `figures._wavg()`, `figures.sankey_daily_aggregates()` 및 리포트 HTML의 JS `_sumRows()`.
  집계 행 스키마 = `[날짜, 물량, 물량×CaO, CaO유효물량, 물량×MgO, MgO유효물량]`.
- 회귀 테스트: `tests/test_edge_cases.py::test_wavg_excludes_blank_grades_*`, `*_sankey_aggregates_ignore_blank_grades`, `*_yard_daily_counts_cao_and_mgo_separately`.

## 7. 매칭 파이프라인 결과 & 한계 ⭐️ (2차: 라인 분리 + 시간인지)

> ⚠️ **2026-07-28 정정**: 아래 표는 원래 **뒤바뀐 페어링**(기존↔CNA, 신설↔45Q)으로 계산되어 있었다.
> 확정 페어링으로 재계산한 결과 **상류 신호는 문서보다 훨씬 약했다**(R² 0.11~0.13 → **0.01~0.02**).
> 재현: `src/models/dataset.build_line_data` + `forecast.evaluate` (07/28 데이터 기준).

**2026-08-05 갱신본 기준 (최신):**

| 라인→야드 | Time-Lag | 상관 | 설명력 천장(R²=corr²) | AR MAE | persist | naive | n |
|-----------|:-------:|:----:|:---------------------:|:------:|:-------:|:-----:|:-:|
| 기존→45Q (4-5K) | 3h | +0.245 | 0.060 | 1.035 | **1.023** | 1.773 | 944 |
| 신설→CNA (6-7K) | 13h | +0.209 | 0.044 | **0.785** | 0.817 | 1.343 | 1,358 |

*(상관은 추정 Time-Lag 지점의 값 = 상류 신호의 **최선값**. 리포트 "정직한 한계" 문구도 이 수치를 인용한다.)*

**2026-07-28 기준 (직전, 비교용):** 기존 3h/+0.110/R² 0.012, 신설 10h/+0.156/R² 0.024.
→ 데이터가 늘며 상관이 다소 올라(0.11→0.25, 0.16→0.21) **신설은 처음으로 persist 를 넘어섰다**
(0.785 < 0.817). 다만 개선폭이 4%에 그쳐 리포트는 여전히 '사실상 동급'으로 표기한다.

- **정직한 한계(천장)**: 상류(광산품위+인출지점)가 야드 CaO 변동의 **4~6%만 설명**(07-28 기준 1~2%에서 상승). 목표(±0.5) 제어에는 여전히 크게 부족.
- **상류 피처는 예측을 개선하지 못함**: AR+상류 MAE가 AR 단독보다 오히려 나쁨(07-28 기준 1.064>1.035, 0.796>0.790)
  → 운영 모델은 `use_upstream=False`(AR 전용)가 타당함이 재확인됨.
- **결론 불변**: 페어링 정정으로 수치는 바뀌었으나 **"상류 신호가 약해 처방적 배합제어는 시기상조"**라는 기존 진단은 오히려 강화됨.
- **원인 진단**: ① 야드 시간의 **25%만** OSP 인출기록 존재(버퍼/직송 미확인) ② 야드 CaO 지속성 큼(자기상관 1h≈0.73) → 상류보다 야드 내부동역학이 지배 ③ 지점→품위가 일단위·75% 커버리지로 거침.
- **신호 강화에 필요한 것(사용자/데이터)**: OSP↔야드 버퍼/직송 여부, 야드 추가 투입원, 인출지점 실측 품위.

- 구현: `src/data/clean.py`(정제) + `src/matching/pipeline.py`(매칭). 스크립트 `scripts/build_matched_dataset.py`.
- 산출: `data/processed/matched_hourly.csv` (148 시간행 × 14열), OSP 예상CaO 매칭 커버리지 100%(라인 433/전역 22).
- **한계(정직 보고)**: OSP 예상CaO ↔ 야드 CaO 상관 0.17 (약함). 원인 가설:
  1. **정적 구역-품위 테이블** — 구역(파일)은 시간에 따라 다른 품위로 재적재되는데 6주 평균을 써 시간정보 소실. → **시간인지(most-recent-loading) 재고 모델**로 개선 필요.
  2. Time-Lag가 상수 아닐 가능성(물량 의존).
  3. 야드 시간내 변동이 큼(시간 집계로 소실).
- **다음 개선**: 구역별 '최근 적재 품위'를 인출시각 기준으로 매칭(재고 추적) → 상관/예측력 향상 기대.

### 6-0-12. 인출 MgO 매칭 & 품위 수지 검증 ⭐️ (2026-08-13 · data_v2_260813)

**갭이었던 것**: `assign_expected_cao_timeaware()` 는 **CaO 만** 인출에 부여했다.
`expected_mgo` 컬럼이 아예 없어 MgO 는 재고 흐름과 대조할 방법이 없었다.

**수정**: `pipeline.GRADE_COLS = {"cao": "expected_cao", "mgo": "expected_mgo"}` 로 일반화.
두 성분을 **같은 asof 매칭 한 번**에서 함께 뽑는다 — 별도로 매칭하면 서로 다른 광산 행이
섞여 성분 조합이 실재하지 않는 값이 된다. 시간창 집계도 `osp_expected_mgo`(톤가중) 를 싣는다.
회귀 테스트: `test_timeaware_assigns_mgo_from_same_source_row` 외 3건.

**품위 수지 검증** (`src/matching/balance.py`, `tests/test_balance.py`)

물량 수지만으로는 매칭의 옳고 그름을 가릴 수 없다 — 엉뚱한 품위를 붙여도 물량은 그대로 통과한다.
그래서 성분량(톤 × 품위)까지 닫고 **기말 재고의 함의 품위**가 물리적으로 가능한지 본다.

검사 설계에서 반드시 지켜야 할 두 가지:

1. **물량 결손을 먼저, 양방향으로 보정한다.** 물량이 안 맞으면 품위 수지가 그 오차를
   그대로 물려받아 음수 품위 같은 헛된 결론이 나온다. 결손이 양수면(적재 초과) 인출 쪽에,
   음수면 적재 쪽에 더한다. **한쪽만 보정하면 반대 부호에서 오판**한다
   (회귀 테스트 `test_one_directional_correction_would_have_failed`).
2. **민감도(지렛대) = 처리량 ÷ 기말재고 를 함께 보고한다.** 이 값이 크면 작은 물량 오차가
   함의 품위를 크게 흔들어 검사 자체가 무뎌진다. 신설은 **41배** — 물량이 1% 어긋나면
   함의 품위가 약 19%p 움직인다.

**결과 (2026-06-10 ~ 08-10, 세 소스가 겹치는 구간)**

| 라인 | 민감도 | 물량결손 | CaO 적재→인출 | CaO 기말함의 | MgO 적재→인출 | MgO 기말함의 |
|---|---:|---:|---|---:|---|---:|
| 기존 | 8배 | +9,114t | 45.81 → 45.26 (−0.55%p) | 49.98 ✅ | 2.92 → 3.05 (+0.13%p) | 1.95 ✅ |
| 신설 | 41배 | −68,056t | 46.10 → 46.20 (+0.10%p) | 41.84 ✅ | 2.70 → 2.65 (−0.04%p) | 4.55 ✅ |

- **부여 커버리지 CaO·MgO 모두 100%** (인출 톤 기준).
- **변동폭 보존** — 기존 CaO 63% · MgO 92%, 신설 CaO 79% · MgO 84%.
  구역-일별 평균을 쓰므로 감쇠는 불가피하나 대부분 살아 있다.
- 보정 후 네 값 모두 물리적 범위 안 → **품위 매칭 자체는 성립**한다.

**⭐️ 남은 문제는 매칭이 아니라 계량이다.** 라인 귀속을 지우고 **두 라인을 합쳐도**
적재 1,323,057t vs 인출 1,386,000t 으로 **−58,943t(4.5%)** 이 남는다. 합산해도 남는다는 것은
원인이 **교차인출(라인 귀속)이 아님**을 뜻한다 (`balance.combined_mass_gap`, 회귀 테스트
`test_combined_gap_survives_line_reattribution`). 계량 배율로는 **β = 1.048**.

**⚠️ 미해결 — 기존 라인 성분 상관 틀어짐** (`balance.component_coherence`)

부여값은 '구역-일별 평균'이므로 비교 기준선도 같은 단위여야 한다(원본 행 단위와 비교하면
집계 감쇠를 매칭 오류로 오인한다).

| 라인 | 원본 행 | 기준선(구역-일별) | 부여 후 | 차이 |
|---|---:|---:|---:|---:|
| 기존 | −0.229 | **−0.159** | **+0.088** | **+0.247** |
| 신설 | +0.198 | +0.082 | +0.025 | −0.056 |

기존 라인은 CaO~MgO 관계의 **부호가 뒤집혔다**. 인출이 특정 구역에 몰려 원본과 다른
조합이 뽑힌 결과로 보인다. **결론: MgO 는 수지 검증용으로만 쓰고 예측 피처로는 아직 쓰지 않는다.**

**미확인 — 교차인출 라인 귀속** (사용자 확인 필요)
`비고`의 `기존->신설 교차인출` 30행 / 87,900t (전체 인출의 6.1%). 기존 시트 18행·50,600t 은
그 라인 인출의 **18.2%** 로 비중이 크다. 두 해석(기록 시트 기준 vs 비고 화살표 기준) 중
어느 쪽인지에 따라 라인별 수지가 달라진다. 다만 위 합산 검사가 보이듯 **총 결손은 어느
해석에서도 남으므로**, 이 질문은 라인별 정밀도 문제이지 총량 문제의 원인은 아니다.

### 6-0-13. 교차인출 귀속 확정 · 품위 출처 플래그 · '5월 재고' 가설 검증 ⭐️ (2026-08-13)

**✅ 교차인출 라인 귀속 — 사용자 확정: 해석 ⓐ (기록된 시트가 곧 그 라인의 인출)**
`비고`의 `기존->신설 교차인출` 표기는 **목적지 메모**이며, 인출 주체는 그 기록이 실린 시트다.
현행 구현(`clean.clean_osp(df, line, pw_cols)` — 라인을 시트 identity 로 주입)이 이미 ⓐ이므로
**코드 변경 없음**. P/W 컬럼 사용 실태(기존 시트=P/W1·2, 신설 시트=P/W3·4)도 이 해석과 일치한다.
→ 해당 Open Issue 종결.

**[신규] 품위 출처 플래그 `grade_source`** (`pipeline.GRADE_SOURCE`)

부여된 품위가 **실측 기반인지 대체값인지** 구별할 수단이 없어, 상당량에 가짜 정밀도가 붙어 있었다.
대체값을 채우기 **전에** 출처를 기록하도록 했다.

| 값 | 의미 | 기존 | 신설 |
|---|---|---:|---:|
| `zone_history` | 그 구역의 실제 적재 이력 | 220,950t (79.3%) | 1,024,100t (88.8%) |
| `line_mean` | 구역 이력이 아직 없어 라인 평균 대체 | 57,750t (**20.7%**) | 127,400t (**11.0%**) |
| `no_zone` | 인출 구역 자체가 미기재 | — | 2,000t (0.2%) |

→ 두 라인 합계 **187,150톤(13.1%)이 대체값**. 리포트 「품위 수지 검증」 섹션에 명시한다.

**❓ 검증: "5월에 남은 재고 = 지금 매칭 안 되는 물량" 가설 (사용자 제안)**

세 갈래로 나눠 검증했고, 결론은 **물량 수지에는 도움이 안 되고, 매칭 품질 개선도 측정 한계 내**다.

*① 물량 수지 — 도움 안 됨 (원리적으로)*
수지식이 `적재 − 인출 = s1 − s0` 이므로 **기초재고 s0 는 이미 식에 들어 있다.**
s0 를 키우면 우변이 더 음수가 되어 결손이 **오히려 커진다**.

| s0 가정 | 기존 결손 | 신설 결손 |
|---|---:|---:|
| 실제 기록값 | +9,114t | −68,056t |
| +20,000t | +29,114t | −48,056t |
| +60,000t | +69,114t | −8,056t |

신설만 보면 s0 +60,000t 에서 닫히지만, **같은 가정이 기존을 7.6배 악화**시킨다. 한 가정으로
두 라인을 동시에 설명할 수 없다. 결정적으로 **실사 재고 추세가 평평**하다
(기존 +1 t/일, 신설 −3 t/일; 범위 각각 10,000~45,000 · 20,000~43,000). 결손 58,943톤은
두 라인 기말재고 합계(63,000t)의 **94%** 규모라, 그만큼 빠져나갔다면 실사값이 0 근처로
갔어야 하는데 그런 하락이 없다. → **기각.**

*② 체류시간 — 5월 물량의 영향은 원리적으로 짧다*

| 라인 | 기초재고 | 인출속도 | 선입선출 소진 | 완전혼합 시정수 |
|---|---:|---:|---:|---:|
| 기존 | 38,000t | 4,639 t/일 | **8.2일** (관측 60일의 13.7%) | 8.2일 → 25일 뒤 잔존 5% |
| 신설 | 29,000t | 18,020 t/일 | **1.6일** (관측 62일의 2.6%) | 1.6일 → 5일 뒤 잔존 1% |

*③ 실제 미매칭 물량은 초반에 몰려 감소한다 — 가설의 현상 자체는 실재*

| 주차 | 기존 미매칭 비율 | 신설 미매칭 비율 |
|---:|---:|---:|
| 1 | 52.1% | 100.0% |
| 2 | 14.6% | 56.0% |
| 3 | 25.0% | 14.7% |
| 4 | 10.1% | 13.8% |
| 5 | 2.9% | 17.7% |
| 6+ | 20.0% → 9.8% | 3.0% |

**감소 패턴은 기초재고 소진의 signature 와 일치**한다. 다만 규모(187,150t)가 기록된
기초재고(67,000t)의 **2.8배**이고, 지속 기간(5주)이 체류시간(1.6~8.2일)보다 **훨씬 길다.**
→ 원인은 '5월 잔여 재고'가 아니라 **구역별 적재 이력이 뒤늦게 시작하는 것**(zone coverage ramp-up).
라인 전체는 06/10부터 기록되지만 개별 구역은 첫 적재 기록이 뜰 때까지 매칭 불가다.

*④ 미매칭 구간을 제외하면 예측력이 좋아지는가 — 측정 결과 개선폭이 노이즈 안*

| 라인 | 최적 lag | A) 전부 사용 | B) 미매칭 제외 | 변화 | 표준오차 |
|---|---:|---:|---:|---:|---:|
| 기존 | 3h | +0.231 (n=60) | +0.249 (n=58) | +0.019 | ±0.13 |
| 신설 | 10h | +0.192 (n=224) | +0.201 (n=217) | +0.009 | ±0.067 |

→ 개선이 **표준오차의 1/7 수준**. 통계적으로 의미 없다. **제외 처리는 예측력 목적으로는 무가치.**

**결론**: 이 가설의 가치는 물량 수지나 예측력이 아니라 **정직성**에 있다. 13.1%가 조용히
라인 평균으로 채워지던 것을 `grade_source` 로 드러냈다. 진짜로 매칭을 개선하려면
**구역별 기초 재고량과 그 품위**가 필요하다(사용자 확인 대기).

### 6-0-14. 현장 답변 반영 — 계량 방식(Q3)·재고 산정(Q5) ⭐️ (2026-08-14)

담당자에게 5개 질문을 냈고 **2개(3번·5번) 답변을 받았다.** 나머지 3개(구역별 기초재고,
적재기록 없는 구역, 인출 순서)는 회신 대기 중이다.

**Q3 — 물량 계량 방식 (원문)**
> 저희는 시설 3호에 있는 벨트 스케일 기준으로 합니다. 각 월 별로 증량 할때도 있고 감량 할 때도
> 있고(공장에서 월마감시 정해줌), 광산·공장 별로 스케일 수치가 똑같지는 않을 겁니다.

⭐️ **해석이 크게 바뀐다.** 종전에는 −58,943톤(4.5%) 결손을 "원인 미상의 불일치"로 다뤘으나,
답변에 따르면 **광산 계량과 공장 계량은 애초에 서로 다른 기기**다. 즉 —
- **0 이 아닌 차이가 정상**이다. 이 값은 '손실'이 아니라 **두 계량 기준의 차이**로 읽어야 한다.
- 월마감 조정이 있으므로 **β 는 월 단위로 따로 추정**해야 하며, 여러 달을 뭉친 평균은 무의미하다.
- 조정은 **공장이 전사적으로 정해주므로**, 두 라인이 **같은 시점에 같은 방향**으로 움직여야 한다.

**검증 — 월 경계에서 두 라인이 같은 방향으로 이동한다**

β = 인출계량 ÷ (적재계량 − Δ실사재고). 1.000 이면 두 계량이 일치.

| 라인 | 2026-06 | 2026-07 | 변화 |
|---|---:|---:|---:|
| 기존 | 1.044 | 0.869 | **−0.176** |
| 신설 | 1.123 | 1.040 | **−0.083** |

**두 라인 모두 6→7월에 하락**했다 — 공장 일괄 조정이라는 설명과 정합한다.
(단, 관측된 월 경계가 1개뿐이라 **정황 일치이지 증명은 아니다.** 8월은 9일치라 제외.)

**월 '안'에서는 안정적인가 — 아직 확인 불가**
10일 단위로 쪼개면 신설 7월이 0.930 · 1.169 · 1.066 으로 흔들린다. 다만 이 산포는
**실사 재고 오차가 지배**한다(Q5 참조): 10일 창의 Δ재고가 ±1,000~12,000톤인데 재고 자체가
1,000톤 단위 개략치라, 4,000톤 오차면 β가 약 0.027 움직인다. **월 이하 해상도로는 판별 불가.**

**Q5 — OSP 재고 산정 (원문)**
> OSP 재고는 49광구 ccp에서 정합니다. 기준은 정해주고 있으나, 사람마다 오차는 있어서
> 정확한 숫자는 아닙니다.

→ 실사 재고는 **기준은 있으나 담당자별 편차가 있는 개략치**다. 따라서 —
- 재고를 기준선으로 쓰는 모든 검사(§6-0-12 기말 함의 품위, 위 β)는 **재고 오차를 물려받는다.**
  특히 신설은 지렛대가 41배라 재고 1,000톤 오차가 함의 품위를 약 1.8%p 흔든다.
- 재고 잔차를 **계량 드리프트의 증거로 단정하면 안 된다** — 사람 오차가 섞여 있다.
- 리포트에 이 한계를 각주로 명시한다(`scripts/make_brief.py` footer).

**남은 질문 (회신 대기)**: ① 구역별 기초 재고량·품위 ② 기존 35번 구역처럼 인출은 있는데
적재 기록이 없는 구역의 사유 ④ 적치더미에서 뽑는 순서(최근분 우선 / 오래된 것 우선).
④ 는 현재 '최근 적재분 우선'으로 구현돼 있고, 거친 대안 비교에서 상관 +0.20~0.22 대 −0.03~+0.04
로 현재 가정이 우세했다(확인 목적의 질문).

### 6-0-15. 현장 답변 반영 ② — 적치장 구조(Q1·Q2)·47Q 구역(Q4) ⭐️ (2026-08-14)

5개 질문 **전부 회신 완료**. 앞선 Q3·Q5 는 §6-0-14.

**Q1 — 구역별 기초 재고 (원문)**
> OSP 구간별 측정은 하지 않고 있음. 6월 10일 기준 재고는 기존 36000톤, 신설 30000톤

- ⭐️ **요청 철회.** 종전 Open Issue "구역별 기초 재고·품위를 받아 미매칭 13.1% 를 해소한다"는
  **원리적으로 불가능**하다 — 그런 데이터가 존재하지 않는다. 추정분은 **메우는 대신
  `grade_source` 로 표시해 두는 것**으로 확정한다(§6-0-13).
- ✅ **재고 기록 검증 통과**: 제시값이 실사 기록의 6/10 마지막 값과 **정확히 일치**
  (기존 36,000 / 신설 30,000). 라인 단위 재고는 신뢰 가능하다는 독립 확인.
  → `S.OSP_OPENING_STOCK`, `S.OSP_ZONE_STOCK_MEASURED = False`

**Q2 — 구역 경계 (원문)**
> OSP 내 구역 구분이 칸으로 구분되어 있는데 칸 높이 이상으로 쌓이다 보면 옆으로 흘러 들어가는
> 경우가 생김. 잔량은 추정을 해야하는 실정임.

⭐️ **매칭의 전제를 건드리는 답변.** 구역 라벨은 엄밀한 경계가 아니라 **근사**다.
→ `S.OSP_ZONE_SPILLOVER = True`
- 관측 증거: 기존 라인 **35번 구역**은 적재 기록 없이 1,250톤이 인출됐다(§6-0-13에서 미상으로
  남겨둔 건). 옆 칸 유입으로 설명된다.
- **⚠️ 시도했으나 실패한 보정 — 인접 구역 혼입 스무딩**
  이웃 구역(±5번) 품위를 가중치 w 로 섞어 부여해 봤다. **처음엔 개선처럼 보였으나 착시였다.**

  | 비교 방식 | 기존 | 신설 |
  |---|---|---|
  | lag 를 각각 재탐색 (❌ 잘못된 비교) | +0.231 → +0.354 | +0.196 → +0.236 |
  | **lag·표본 고정 후 짝비교** (✅ 옳은 비교) | +0.213 → +0.073 | +0.187 → +0.102 |

  lag 를 다시 찾으면 **표본이 줄면서(n 60→32) 다른 lag 가 뽑혀** 상관이 커 보였다.
  같은 lag·같은 표본에서 짝지어 부트스트랩(2,000회)하면 **Δr 이 음수**이고 95% CI 가 0을
  포함한다(기존 −0.140, CI −0.364~+0.095 / 신설 −0.084, CI −0.203~+0.031).
  → **단순 인접 스무딩은 개선이 아니다.** 넘침이 상시 균일 혼합이 아니라 **과적재 시에만
  국소적으로** 일어나기 때문으로 보인다. 채택하지 않는다.
- **결론**: 구역 단위 품위 매칭의 정밀도에는 **물리적 상한**이 있다. 이는 표본을 늘려서
  넘을 수 있는 종류가 아니므로, **인출 지점 실측**의 근거가 하나 더 늘었다.

**Q4 — 인출·적재 순서 (원문)**
> OSP 90,100번 위치에서 10000~15000톤 인출하는데 이 물량은 47Q 물량일 가능성이 약 80% 이상임.
> 인출 후 90번 이전 위치에서 나머지 만실(47000톤)양 까지 채운다.

✅ **데이터가 강하게 뒷받침한다.**

| 소스 | 90번 이상 적재량 | 그 소스 전체 대비 |
|---|---:|---:|
| 47Q | 225,322톤 | **73.4%** |
| 49Q | 10,106톤 | **1.0%** |

73배 편중 — "90번 이상은 47Q 물량"이 데이터로 확인된다. → `S.OSP_47Q_ZONES = (90, 95, 100)`

- **인출 규모**: 회당 약 1,500톤이라 언급하신 10,000~15,000톤과 다르지만, **연속 캠페인으로
  묶으면 일치**한다 (기존 06/22~07/04 13,600톤 / 신설 07/08~07/18 18,200톤 · 07/29~08/12 17,850톤).
  즉 '한 번 뽑을 때'가 아니라 **한 차례 인출 기간**을 말씀하신 것으로 읽힌다.
- **만실 47,000톤 교차검증 통과**: 실사 재고 최고치가 기존 45,000 · 신설 43,000 으로
  **한 번도 넘지 않았다**(n=2,567 / 2,557). 독립 출처 두 개가 맞아떨어진다.
  → `S.OSP_CAPACITY_TON = 47_000` (배합 최적화의 재고 상한 제약으로 사용)
- 구역 100 은 **인출 기록이 없다**(적재만 있음). 90번만 인출된다.

**[🐞 수정] 구역-일별 품위가 단순평균이었다 → 톤가중으로 교정**

Q4 검증 중 발견했다. 구역 90 에 49Q(1,299톤, 49.85%)와 47Q(7,820톤, 45.71%)가 함께 적재되는데,
단순평균은 두 값을 1:1 로 섞어 **소량 기록에 과도한 무게**를 준다. 인출 시 실제로는 **톤 비율대로**
섞여 나오므로 톤가중이 물리적으로 옳다.

- 영향: 구역-일 296건 중 **26건(8.8%)이 0.3%p 이상** 어긋났고 **최대 1.61%p**. 평균 절대차 0.082%p.
- 하류 상관 변화는 −0.017 / −0.010 로 **노이즈 범위**이나, **정확성 문제이므로 교정**한다.
- 품위 수지는 개선됐다: 신설 기말 함의 CaO **41.84 → 43.57%** (물리적 중심에 더 가까움).
- 구현 `pipeline._weighted_daily` (물량이 없거나 0 이면 단순평균으로 물러섬).
  회귀 테스트 `test_zone_daily_grade_is_tonnage_weighted`, `test_weighted_daily_falls_back_without_tonnage`.

### 6-0-16. 배합 처방 구축 & ⭐️구역 귀속 붕괴 발견 (2026-08-14)

사용자 의도를 그대로 산출하는 최종 형태를 만들었다 —
**"OSP 몇 번에서 몇 톤을, 지금부터 몇 시간 뽑으면 기준값에 드는가."**

**신규 모듈**
- `src/matching/inventory.py` — 구역별 재고 **복원**. 구역 재고는 측정하지 않으므로(§6-0-15 Q1)
  `기초_i + 누적적재_i − 누적인출_i` 로 만들고, **라인 합계를 실사에 맞춰 재정규화**한다.
  기초는 초기 14일 적재 비중으로 배분.
- `src/optimization/prescribe.py` — 처방 + **규격 적중 확률**. 구역 품위의 표준오차를 배합
  비중으로 전파해 `blend_se` 를 만들고 정규 근사로 P(목표±허용폭)를 낸다.
  ⭐️ **확률 없는 처방은 내지 않는다** — 배합 LP 는 쉽게 풀리지만 넣는 품위가 부정확하면
  "계산상 44.6%" 는 공허하기 때문이다.
- `scripts/make_prescription.py` → `outputs/prescription.html` (18KB)
- `src/visualization/webstyle.py` — 브리프/처방서 공통 스타일 토큰

**실행 결과 (2026-08-13 기준)**

| 라인 | 처방 | 예상 CaO | 배합오차 | **적중 확률** | 달성 |
|---|---|---:|---:|---:|:--:|
| 기존 | 95번 339t + 20번 165t (8시간, 504t) | 44.60% | ±0.88%p | **43%** | ✅ |
| 신설 | 50번 2,239t + 75번 2,168t + 30번 730t + 45번 710t + 15번 258t (6,105t) | 45.12% | ±0.36%p | **47%** | ❌ 저품위 재고 부족 |

→ **LP 가 목표를 정확히 맞춰도 실제 적중은 동전 던지기 수준**이다. 운전에 쓰려면 80%+ 필요.

**⭐️⭐️ 근본 원인 — 구역 귀속이 붕괴해 있다**

구역별 적재량과 인출량을 맞춰 보면 어긋난다. `prescribe.zone_balance_health` 로 상시 감시한다.

| 라인 | 이상 구역 | 이상 구역이 차지하는 인출 | 최악 배율 |
|---|---:|---:|---:|
| 기존 | 11 / 17 | **55%** | **21배** |
| 신설 | 8 / 19 | 28% | 7배 |

패턴이 뚜렷하다 — **적재는 95·100번에 몰리고 인출은 60~90번에서 일어난다.**
(기존 95번: 적재 63,138t / 인출 2,200t · 90번: 적재 2,357t / 인출 39,500t)

→ **인출 기록의 구역 번호는 '그 물량이 적재된 곳' 을 가리키지 않는다.**
이는 §6-0-15 의 세 답변과 전부 정합한다 — 90·100에서 뽑고 90번 이전을 채운다(Q4),
칸이 넘치면 옆으로 흘러든다(Q2), 구역별 재고는 측정하지 않는다(Q1).
**구역 단위 품위 매칭의 상관이 0.2 를 못 넘는 이유가 여기 있다.**

**시도했으나 채택하지 않은 대안 — 라인 전체를 하나의 더미로 보는 완전혼합 모델**

구역 라벨을 아예 버리고 `dG/dt = (적재율/I)(g_in − G)` 로 라인 평균 품위를 적분해 봤다.

| 라인 | 현재(구역별) | 대안(더미 혼합) | 표본 |
|---|---:|---:|---|
| 기존 | **+0.211** (lag 3h, n=60) | +0.148 (lag 18h, n=253) | 구역 우세 |
| 신설 | +0.191 (lag 12h, n=220) | **+0.245** (lag 0h, n=1,218) | 더미 우세 |

라인마다 우열이 갈리고 **둘 다 설명력 4~6%** 에 그쳐, 지금 교체할 근거가 없다. 보류한다.

**결론**: 처방을 만드는 **계산 장치는 완성되어 동작한다.** 남은 것은 **넣을 값의 정확도**이며,
가장 큰 지렛대는 ① 인출 지점 실측 ② **인출 기록에 '어디서 쌓은 물량인지' 병기**(설비 불필요)
③ 월마감 계량 조정값 공유다.

## 5. 목표 & 제약 + ⭐️핵심 발견

- **목표**: 야드 적재 CaO 평균 44.6%, std ≤ 0.5
- **CaO 실측 통계**:
  | 소스 | n | 평균 | std | min | max |
  |------|---|------|-----|-----|-----|
  | 49Q광산 | 141 | 46.09 | 2.96 | 38.7 | 51.9 |
  | 47Q광산 | 59 | 45.56 | 0.46 | 43.9 | 46.7 |
  | **CNA야드(최종)** | 20,952 | **44.67** | **1.85** | 25.0 | 54.2 |
  | 45Q야드 | 3,839 | 45.40 | 2.26 | 38.4 | 53.0 |
- ⭐️ **야드 CNA 평균(44.67)은 이미 목표(44.6)에 근접 — 진짜 문제는 '분산(std 1.85)'을 0.5로 줄이는 것.**
  → ML 목표는 평균 이동이 아니라 **변동성 제어(안정화)** 에 초점.
- CNA min 25.0 = 센서 드롭아웃/이상치 가능성 → 검증 게이트 점검 대상.

## 6. 데이터 품질 이슈 (정제 TODO)
1. 49Q `이송물량(톤)` object형 + '-'(운휴) → numeric 변환
2. OSP적재구역 복합코드(예: "50/45/50/45/55") 파싱/전개 규칙 필요
3. OSP신설 `공정구분` 전부 NaN → '신설' 보정
4. 시각 컬럼(문자열) → time 파싱, 일자와 결합해 datetime 생성
5. 야드 `Yard` 컬럼 전부 결측 → 제거 검토
6. CNA 이상 저품위(25.0 등) 처리 방침 필요
