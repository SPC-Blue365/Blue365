# 데이터 스키마 & 매칭 규칙 (Data Schema & Matching Rules)

> `[Data-Analyst]` EDA 1차 결과. 원본: `data/raw/data_v1.xlsx` (8개 시트, 기간 2026-06-01 ~ **07-28**, 2026-07-28 갱신).
> ✅=데이터로 확인된 사실 / ❓=사용자 확인 필요(추측 금지, CLAUDE.md §2).

---

## 0. 야드변경 시트 (2026-07-24 추가) ⭐️
`기존라인 야드변경`(9행)·`신설라인 야드변경`(22행) — 컬럼: `변경일·변경시간·Yard·석회석CaO·석회석MgO·야드물량`.
- 각 행 = 야드 변경 이벤트(그 시점부터 해당 야드에 실제 적재한 물량·품위). 야드: 기존(Y1/Y2), 신설(Y1/Y2-내수).
- 용도: **야드변경 기반 추적 Sankey(라인→야드, 링크=야드물량, CaO/MgO 토글)** + 변경일자별 품위 추이. (누적 인출물량 대체 뷰)
- 정제: `clean.clean_yard_change`, 로더: `dataset.load_yard_change`.

## 1. 데이터 소스 개요 (원 6개 시트 + 야드변경 2개 + OSP 재고 1개 = **9개**)

> 📅 **2026-08-05 갱신본 기준 행수·기간** (아래 표는 최초 작성 시점 값이라 낡았다 — 최신은 이 블록):
> 49Q 223행(06-10~08-03) · 47Q 90행(06-11~08-03) · OSP인출 기존 103행(06-10~08-02) /
> 신설 255행(06-01~08-03) · CNA 28,907행(06-10~**08-05**) · 45Q 3,839행(06-11~07-21, **불변**) ·
> 야드변경 기존 9건(~07-15, 불변) / 신설 28건(~08-04) · OSP 재고 2,557행(2024-04-09~08-05).


| 공정 | 시트명 | 행수 | 성격 | 기간 |
|------|--------|------|------|------|
| ① 광산 | `광산(49Q)_XRF` | 153 | 채굴 원위치 품위 (XRF), 교대(1·2·3차)/일 | 06-10~07-22 |
| ① 광산 | `광산(47Q)_감마레이` | 71 | 채굴 원위치 품위 (감마레이), 시작~종료시각 | 06-11~07-21 |
| ② OSP | `OSP인출_기존` | 91 | 기존라인 인출 이벤트 (시각별) | 06-10~07-11 |
| ② OSP | `OSP인출_신설` | 150 | 신설라인 인출 이벤트 (시각별) | 06-01~07-14 |
| ③ 야드 | `CNA data` | 20,952 | CNA 실시간 분석기 (약 2분 간격) | 06-10~07-21 |
| ❓ | `45Q 감마레이 data` | 3,839 | 연속측정(야드형 구조) — 정체 확인 필요 | 06-11~07-21 |

## 2. 핵심 컬럼

### ① 광산 (Mine)
- `채굴일자`(date), `채굴시간(교대)`[1·2·3차] 또는 `시작시간`/`종료시간`
- `공정구분` = **기존 / 신설 / 운휴** (라인 구분) ✅
- `OSP적재구역` = 채굴물을 적재한 품위 구역코드 ✅ (복합코드 존재: "50/55", "55-45", "100-0" 등 → 한 교대에 여러 구역 적재)
- `CaO품위`, `MgO품위`(%), `이송물량(톤)` (49Q는 object형+'-' 혼입 → 정제 필요)

### ② OSP (인출)
- `일자`(date), `인출시간`(time)
- `공정구분`: 기존시트=전부 '기존' ✅ / 신설시트=전부 NaN(시트명으로 '신설' 보정 필요)
- `P/W1호~P/W4호` = **인출 품위 구역코드**로 추정(값 20~95, 광산 OSP적재구역과 동일 어휘) ❓
  - 기존라인 → P/W1·2호 사용 / 신설라인 → P/W3·4호 사용 ✅ (구조적 분리 확인)
- `인출량`(톤) = 그 인출 회차 총량 ✅ (구역별 배분 규칙 ❓)

### ③ 야드 (Yard)
- `일자`, `시간`, `CaO`, `MgO`(%), `적재물량` (CNA=TPH / 45Q=B/S, 단위 상이 ❓)
- `Yard` 컬럼: 두 시트 모두 전부 결측 → 미사용/플레이스홀더

## 3. Join Key (식별자) — ✅ 확정 (사용자 확인)

| 축 | Key | 규칙 |
|----|-----|------|
| 라인 | `공정구분` (기존/신설) | 기존→P/W1·2호, 신설→P/W3·4호 |
| 시간 | 일자 + 시각 → datetime | 시간창(1h) 집계 후 Time-Lag 정렬 |
| 공간(품위) | `OSP적재구역` ↔ `P/W호` (구역 20~95) | 광산 구역-품위 평균 → OSP 인출에 예상 CaO 부여 |
| 배분 | 인출량 ÷ 구역수 (균등) | 사용자 확정 |
| 최종 목표 | `CNA data` | 야드 적재 품위 기준 (45Q/47Q/49Q = 채굴구역 3곳) |

## 4. Time-Lag 규칙 — 🔬 데이터 추정 (1차)

- 방법: OSP 시간별 예상CaO를 지연시키며 야드 CaO와 피어슨 상관 최대 지연 탐색 (`estimate_time_lag`).
- **1차 결과: 최적 지연 ≈ 11시간, 그러나 상관 0.17로 약함.** → §7 참조. 매칭 신호 강화 필요.

## 6.5 도메인 확정 (2026-07-23, 사용자) ⭐️ 중요 정정
- **기존 ≠ 신설 = 서로 다른 야드**: 기존 인출 → **4-5K 킬른** 야드 / 신설 인출 → **6-7K 킬른** 야드. → 절대 합치지 않고 라인별 분리 모델링.
- **P/W1~4호 = 인출 지점(위치)**, 품위 등급 아님. 지점당 50% 균등 인출. (구역숫자 20~95는 위치ID)
  - 검증: 구역숫자 vs CaO 상관 = -0.001, 구역 내 CaO std 2.58 → 숫자는 품위 무관 확인.
- **품위 매칭은 시간인지로**: 지점의 품위는 '그 지점에 최근 적재된 광산 품위'(merge_asof)로 부여.
- **야드 페어링(✅ 사용자 확정)**: 기존↔`45Q 감마레이 data`(4-5K 킬른), 신설↔`CNA data`(6-7K 킬른).
  - 초기에 데이터 상관만 보고 반대로 추정했으나(기존↔CNA), **사용자 도메인 확정이 우선**하여 정정됨.
  - **정본은 `config/schema.py`의 `YARD_PAIR` 하나뿐**이다. 스크립트·노트북에서 야드 시트를 직접 지정하지 말 것
    (`scripts/build_matched_dataset.py`가 옛 페어링을 하드코딩해 잘못된 매칭셋을 만든 버그가 실제로 있었음 — 2026-07-28 수정,
    회귀 테스트 `tests/test_edge_cases.py::test_scripts_do_not_redefine_yard_pairing`로 고정).

### 6-0. 채굴 교대 시간대 & 광산 대표 시각 ⭐️ (2026-07-28 사용자 확정)

광산 데이터에는 원래 시각이 없어 일 단위로만 배치했고, 구간 경계에서 그 날 채굴분이
통째로 빠지거나 하루 전체가 들어가는 문제가 있었다(실측 평균 27.9%, 최대 100% 차이).
사용자 확정 교대 시간대로 **대표 시각**을 부여해 해소했다.

| 교대 | 시간대 | 대표 시각(중점) | 49Q 건수 |
|------|--------|:---------------:|:--------:|
| 1차 | 08:00~16:00 | **12:00** | 61 |
| 2차 | 16:00~24:00 | **20:00** | 61 |
| 3차 | 00:00~08:00 | **04:00** | 61 |

- **대표 시각 = 교대 중점**. 시작 시각을 쓰면 경계에서 최대 8시간을 앞당기게 되므로 중점이 오차가 가장 작다.
- **47Q는 실측 시작·종료 시각**이 있으므로 그 **중점**을 쓴다(구간 평균 3.65시간). 종료<시작이면 자정 넘김으로 보고 하루를 더한다.
- 정의는 `config/schema.py`의 `SHIFT_HOURS`, 구현은 `clean.shift_midpoint()`·`clean._time_midpoint()`.
- **인수인계 30분은 반영하지 않는다(`HANDOVER_APPLIED=False`)** — 대표 시각이 15분 움직일 뿐이고,
  33개 야드변경 구간 **전부에서 귀속이 바뀌는 행이 0건**이었다(최소 구간 40시간 대비 0.62%).
  필요해지면 `HANDOVER_APPLIED=True` 한 줄로 켤 수 있다.
- **총량은 보존된다**: 시각 부여 후에도 결측 0건, 라인·소스별 합계가 원본과 100% 일치.

### 6-0-2. OSP 실사 재고 시트 ⭐️ (2026-07-30 추가)

시트명 `OSP 재고`. **좌우 2블록**이 나란히 있고 가운데(4번 열)는 빈 칸이다.

| 블록 | 열 | 라인 | 행수 | 기간 |
|------|----|------|-----:|------|
| OSP1 | 0~3 | **기존** | 2,525 | 2024/04/09 ~ 2026/07/30 |
| OSP2 | 5~8 | **신설** | 2,515 | 동일 |

- 헤더는 **3번째 행**(index 2): `날짜 · 차수 · OSP 구분 · 재고량`. 데이터는 4번째 행부터.
- **차수 = 채굴 교대**(1차 08~16 · 2차 16~24 · 3차 00~08)이고, **재고 실사는 교대 중간에 1시간 동안** 이뤄진다.
  대표 시각은 그 파악 시간대의 중점을 쓴다: **1차 12:30 · 2차 20:30 · 3차 04:30** (`STOCK_TAKE_HOURS`).
  → 채굴 대표 시각(12:00/20:00/04:00, §6-0)과 **다르다.** 혼동 금지.
- 하루 안 기록 순서는 `3차 → 1차 → 2차`이며, 대표 시각으로 정렬하면 시간순과 일치한다.
- **실사값은 1,000톤 단위 개략치**다(전 구간 100%가 1,000의 배수, 고유값 27개). 절대 수준·추세 파악에는 쓰되 정밀 대조에는 부적합.
- **중복**: 같은 (라인·날짜·차수)가 두 번 기록된 경우가 전 기간 14건. **마지막 기록을 채택**하고 검증 게이트가 건수를 보고한다. *(모두 2026-06 이전이라 현재 분석 기간에는 영향 없음)*
- **결측**: 최근 실사 미입력 2건(2026-07-30 1차). 결측으로 유지한다.
- 구현: `clean.clean_osp_stock()`, `dataset.load_osp_stock()`, 대조 `dataset.stock_vs_flow()`.

#### ⚠️ 미해결: 실사 재고와 흐름 계산의 큰 격차

| 라인 | 실사 변화 | 흐름 계산(적재−인출) | 격차 | 일평균 |
|------|----------:|--------------------:|-----:|-------:|
| 기존 | -6,000톤 | -20,573톤 | **+14,573톤** | +370톤/일 |
| 신설 | +4,000톤 | -72,344톤 | **+76,344톤** | +1,624톤/일 |

실사 재고는 정상 범위(기존 28~45천톤, 신설 22~40천톤)를 유지하는데, 광산 적재−인출로 계산하면
**신설은 재고가 마이너스**가 되어야 한다(물리적으로 불가능). 즉 **광산(49Q·47Q) 기록에 잡히지 않는
유입이 있거나, 인출 기록의 기준이 다르다.** 원인 미확정이므로 **어느 한쪽을 맞다고 단정하지 않고
둘 다 표시**한다(§2-1). → 사용자 확인 대기 항목.

### 6-0-3. 인출 계량 = 벨트스케일 & 지시 배율 β ⭐️ (2026-07-30 사용자 확인)

**OSP 인출량은 벨트스케일(belt scale)로 계량한다.** 벨트스케일 오차는 통과 물량에
**비례**하므로, 실사 재고와의 격차를 배율 **β = 실제 ÷ 계량기 지시값** 으로 표현하는 것이
물리적으로 타당하다(가산 보정보다 이 형태가 실제로 잔차도 작았다).

| 라인 | 전체 β | 95% 신뢰구간 | 해석 |
|------|------:|--------------|------|
| 기존 | **0.927** | 0.921 ~ 0.933 | 계량기가 약 **7.9% 과대 지시** |
| 신설 | **0.906** | 0.903 ~ 0.910 | 계량기가 약 **10.4% 과대 지시** |

**10일 단위 β 추이** (오차막대 = 95% CI, 겹치지 않으면 실제 변화):

| 라인 | 구간별 β | 판정 |
|------|----------|------|
| 기존 | 0.855 → 0.968 → 0.998 → **1.040** | **한 방향으로 상승**(10일당 +0.058) — 지시값이 점점 변함, **교정 점검 대상** |
| 신설 | 0.882 → 0.810 → 0.973 → 0.824 → 0.888 | 추세 없이 **들쭉날쭉**(0.81~0.97) — 그때그때 조건에 따라 흔들림 |

- 두 라인 모두 인접 구간의 신뢰구간이 겹치지 않아 **노이즈가 아닌 실제 변동**이다.
- 구현: `dataset.calibrate_flow()`(전체 β·표준오차), `dataset.calibration_windows()`(구간별 β),
  `figures.calibration_drift()`(추이 차트).
- ⚠️ 실사 재고도 육안이라 오차가 있으므로, β 는 **'계량기만의 오차'가 아니라 두 기록 간 상대 배율**이다.
  다만 실사 오차는 무작위(평균 0)에 가깝고 β 는 체계적이라, 계량 쪽 요인이 지배적일 가능성이 높다.

### 6-0-4. OSP 적재구역 코드 표기 ⭐️ (2026-08-05 갱신)

한 회차에 여러 구역을 적재하면 코드가 복합 문자열로 들어온다. **구분자를 놓치면 구역이
조용히 사라지므로**(숫자 변환 실패 → 토큰 폐기) 관측된 표기를 모두 정본에 반영한다.

- 구분자: `/` · `-` · `~` · **`,`(2026-08-05 갱신본에서 처음 등장)** · 공백
  → `config.schema.ZONE_DELIMITERS = r"[/\-~,\s]+"`
- 실제 관측 예: `50/55` · `55-45` · `100-0` · `15~35` · `95-85-80` · `65/40/70/20-80` ·
  `55/75/50/75/40~45/75` · **`25~30, 65~40`** · **`35~60, 75~85`** · `60~` · `~70`
- `운휴`(가동 안 함)와 빈칸은 구역 없음(`[]`)으로 처리하되, **물량이 있으면 행 자체는 살린다**
  (`zone=NaN`) — 구역만 모르는 것이지 물량이 없는 게 아니다.
- 회귀 테스트: `tests/test_edge_cases.py::test_zone_codes_split_on_comma`

### 6-0-5. β 를 식별할 수 없는 구간 ⭐️ (2026-08-05 확립)

라인이 멈춘 구간에서는 인출이 거의 없는데 재고는 움직여, 배율 β 의 분모가 0에 가까워지고
값이 몇 배로 튄다(실제로 기존 라인 07/21~07/31 창이 **β=6.98**로 나왔다).

- **판정 기준**: 창의 인출 최대 누적이 `MIN_WINDOW_TON = 5,000톤` 미만이거나,
  β 가 타당범위 `PLAUSIBLE = (0.7, 1.3)` 밖이면 → `ok=False`
- **처리**: 그래프·추세 판정에서 제외하되 **몇 개를 왜 뺐는지 리포트 본문에 명시**한다(§2-1).
  조용히 지우지 않는다.
- 정의 위치: `src/models/dataset.py` (`calibration_windows`)
- 회귀 테스트: `tests/test_edge_cases.py::test_calibration_window_rejects_unidentifiable_beta`

### 6-0-6. 기존 라인 미가동 구간 (2026-07/21~07/28) ⭐️ 사용자 확정 (2026-08-05)

- **기존 라인은 가동하지 않았다.** 채굴분은 **OSP 재고를 채우는 용도**로 적재만 했고 **인출은 없다.**
- `OSP인출_기존`의 07/20→07/29 공백은 **기록 누락이 아니라 실제 미가동**이다 → 인출 0 이 참값.
- 결과: 이 구간은 **인출·벨트스케일 오차가 개입하지 않는 깨끗한 검증창**이다.
  `Δ재고 = 적재` 항등식이 그대로 성립하므로 **광산 적재 기록을 직접 감사**할 수 있다.
- 이 구간에서 β(계량 배율)는 **분모가 0이라 원리적으로 식별 불가** → §6-0-5 가드가 자동 제외한다.
- 감사 결과(07/21 04:30 → 07/27 20:30, 실사 Δ**+10,000톤**):

  | 47Q 버전 | 기존 적재 | 신설 적재−인출 | 기존 불일치 | 신설 불일치 |
  |---|---:|---:|---:|---:|
  | 갱신 전(07-30본) | 18,966 | +5,990 | **+8,966** | −3,010 |
  | 갱신 후(08-05본) | 26,738 | **−1,844** | **+16,738** | −10,844 |

  → **총 물량은 두 버전이 동일**(160,957 vs 160,893톤)하고 총 불일치도 같다(+5,957 / +5,893).
  차이는 오직 **라인 배분**이며, 갱신 후는 ⓐ 기존 불일치를 2배로 키우고
  ⓑ 신설을 **적재<인출(−1,844)로 만들어 재고가 +9,000톤 늘어난 실사와 모순**시킨다.
- **용량 제약**: OSP1 재고는 이 구간 32,000→최대 44,000톤, 역대 최대 45,000톤.
  즉 담을 수 있는 여유가 **약 13,000톤**뿐인데 갱신 후 기록은 26,738톤을 적재했다고 한다 → **물리적으로 불가능**.
- **결론**: 47Q 재갱신의 **형식·시간해상도·품위정밀도는 개선**이지만, **`공정구분`(라인 귀속)은 실사 재고와 배치**된다. 현장 재확인 대상.

### 6-0-7. 복합 라인 라벨 & 라인 귀속 정정 ⭐️ (2026-08-05)

**① 복합 `공정구분` 라벨 (버그 수정)**
광산 시트의 `공정구분` 에 한 교대가 두 상태에 걸친 값이 들어온다. 이전에는 라인명과
달라 **라인별 집계에서 통째로 빠졌다 — 7행 21,065톤(광산 총량의 1.7%).**

| 라벨 | 물량 | 가동 시간 | 근거 |
|---|---|---|---|
| `운휴/기존` · `운휴/신설` | 뒤쪽 실제 라인에 **전량 귀속** | **교대의 50%** (`IDLE_ACTIVE_RATIO`) | ✅ 사용자 확정 — "절반은 운휴, 절반만 이송" |
| `기존/신설` | 두 라인에 **균등 배분** | 100% | ✅ 사용자 확정 — 구역 `45/70` 이어도 위치 대응이 아니라 균등 배분 |
| `운휴` 단독 | 행 제외 | 0% | 실물량 없음 |

> ⭐️ `운휴/X` 의 **물량은 전량 X 라인 것**이고 바뀌는 것은 **가동 시간**뿐이다.
> 정제 결과에 `active_ratio` 컬럼으로 실려 나가며, 생산능력(C/R) 상한과 비교할 때 쓴다.
> 실제로 복합 라벨 6행을 4시간 가동으로 환산하면 473~1,191 t/h 로 모두 능력 상한 안에 들어온다
> (8시간으로 보면 236~596 t/h 로 비정상적으로 낮다) — 해석의 정합성이 확인된다.

- 정의: `config.schema.LINE_SPLIT_DELIM` · 구현: `clean.split_line_label`
- **검증 게이트에 `line_unknown` 검사 추가** — 알 수 없는 라벨이 남으면 물량과 함께
  ERROR 로 보고한다. 다시는 조용히 빠지지 않는다.
- ⚠️ `기존/신설` 의 균등 배분은 **가정**이다. 구역과 라인이 위치로 대응한다면
  (예: 구역 `45/70` → 45=기존, 70=신설) 배분이 달라진다 — 현장 확인 대상(해당 1행 5,762톤).
- 회귀 테스트: `test_composite_line_labels_are_split_not_dropped`, `test_validation_flags_unknown_line_label`

**② 47Q 라인 귀속 정정 (사용자 확정)**
§6-0-6 감사 결과와 사용자 확정("갱신 전 기준이 맞다")에 따라, 08-05 갱신본이
신설→기존으로 옮긴 블록을 되돌린다.

- 방법: 갱신 전 기록에 **시간 겹침 다수결**로 대응 → 4개 블록만 바뀐다(추측 없음).
- 정의: `config.schema.MINE_LINE_OVERRIDES` — **시각·라인 라벨만** 담는다(품위·물량은 원본 그대로).
- 적용: `clean.apply_line_overrides` (47Q 전용). 규칙이 원본과 안 맞으면 **경고**한다.
- 결과: 07/22~07/27 기존 라인 47Q 15,007 → **7,797톤** (갱신 전 원본 7,173톤과 624톤 차 —
  블록 경계가 달라 생기는 잔차이며, 그 세분화 자체는 갱신본의 개선점이다).
- 재고 수지 개선: 기존 불일치 +16,738 → **+9,527톤**, 신설 −10,844 → **−3,634톤**,
  신설이 다시 적재>인출이 되어 **재고 증가 방향과 부합**(모순 해소).
- ⚠️ **원본 시트의 `공정구분` 이 현장에서 바로잡히면 이 표를 지워야 한다.**

### 6-0-8. 생산방법 (C/R) & 생산능력 ⭐️ (2026-08-05 사용자 사전 고지 — 다음 갱신 반영 예정)

**다음 갱신본에 `C/R`(생산방법) 컬럼이 추가된다.** 값과 시간당 생산능력:

| C/R | 설비 | 시간당 생산능력 |
|---|---|---|
| `G/C` | 자이레토리 크러셔 (Gyratory Crusher) | **1,500 ~ 1,600 t/h** |
| `H/C` | 함마 크러셔 (Hammer Crusher) | **1,000 ~ 1,100 t/h** |
| `G/C+H/C` | 두 크러셔 **동시 가동** | **1,500 ~ 1,600 t/h** |

> ⭐️ **동시 가동이 가산되지 않는다** — 1,600 + 1,100 ≠ 2,700 이고 G/C 단독과 같다.
> 크러셔 **하류에 병목**(컨베이어·이송 계통)이 있다는 뜻이며, 동시 가동은 처리량이 아니라
> **여유·이중화**를 얻는 운전이다. 배합 최적화의 생산 제약식도 이 형태로 세워야 한다
> (`min(설비합, 하류상한)`, 단순 합산 금지).

정의: `config.schema.CRUSHER_CAPACITY_TPH` · `COL_CRUSHER` · 테스트 `test_crusher_capacity_spec_is_not_additive`

**다음 갱신 때 할 일 (2가지)**
1. **능력 상한 감사** — 교대별 적재 기록을 `물량 ÷ (8h × active_ratio)` 로 환산해 해당 C/R 상한과 대조.
   상한을 넘는 기록은 물리적으로 불가능하므로 기록 오류 후보다.
   *사전 점검(C/R 컬럼 없이 49Q 159개 교대)*: **G/C 상한 1,600 t/h 초과 0건**, 최대 **1,564 t/h** 로
   상한에 근접 → **사용자 제시 능력치가 실측과 정합**. H/C 상한 초과 32건(20%)은 G/C·동시가동 구간일 것으로
   보이며, C/R 컬럼이 오면 바로 확인된다. (상위 기록은 모두 **3차 교대** — 야간 고생산.)
2. **재고 추이 재분석** — 생산방법별 처리율로 적재 곡선을 세분화한다. 현재 미해결인
   **총 적재가 실사 Δ재고보다 +5,900톤 많은 문제**(§6-0-6)를 능력 상한 관점에서 다시 검증할 수 있다.

### 6-1. 결측(빈칸) 품위 처리 규칙 ⭐️ (2026-07-28 확립)

원본에 **품위가 빈칸인 행**이 존재한다(물량은 있는데 분석값이 없는 경우).

| 소스 | CaO 결측 | MgO 결측 | 비고 |
|------|:--------:|:--------:|------|
| 광산 49Q(XRF) | 1행 (0.5%) | 1행 (0.5%) | 영향 미미 |
| **광산 47Q(감마)** | **12행 (15.0%)** | **12행 (15.0%)** | **물량의 13.6%** |
| OSP 인출 | 0% | — | |
| 야드 CNA(신설) | 0% | 0% | |
| **야드 45Q(기존)** | 0% | **521행 (13.6%)** | MgO만 결측 |
| 야드변경 | 0% | 0% | |

- **규칙**: 품위 빈칸 행은 **물량(합계)에는 포함**하되, **평균 계산의 분자·분모 양쪽에서 제외**한다.
  빈칸을 0으로 넣으면 평균이 0쪽으로 끌려 내려간다(실제로 47Q CaO가 45.61 → 36.63으로 왜곡됐던 버그).
- **CaO와 MgO는 결측 위치가 다르므로 성분별로 유효물량·유효건수를 따로 집계**한다(45Q는 MgO만 결측).
- 구현: `figures._wavg()`, `figures.sankey_daily_aggregates()` 및 리포트 HTML의 JS `_sumRows()`.
  집계 행 스키마 = `[날짜, 물량, 물량×CaO, CaO유효물량, 물량×MgO, MgO유효물량]`.
- 회귀 테스트: `tests/test_edge_cases.py::test_wavg_excludes_blank_grades_*`, `*_sankey_aggregates_ignore_blank_grades`, `*_yard_daily_counts_cao_and_mgo_separately`.

## 7. 매칭 파이프라인 결과 & 한계 ⭐️ (2차: 라인 분리 + 시간인지)

> ⚠️ **2026-07-28 정정**: 아래 표는 원래 **뒤바뀐 페어링**(기존↔CNA, 신설↔45Q)으로 계산되어 있었다.
> 확정 페어링으로 재계산한 결과 **상류 신호는 문서보다 훨씬 약했다**(R² 0.11~0.13 → **0.01~0.02**).
> 재현: `src/models/dataset.build_line_data` + `forecast.evaluate` (07/28 데이터 기준).

**2026-08-05 갱신본 기준 (최신):**

| 라인→야드 | Time-Lag | 상관 | 설명력 천장(R²=corr²) | AR MAE | persist | naive | n |
|-----------|:-------:|:----:|:---------------------:|:------:|:-------:|:-----:|:-:|
| 기존→45Q (4-5K) | 3h | +0.245 | 0.060 | 1.035 | **1.023** | 1.773 | 944 |
| 신설→CNA (6-7K) | 13h | +0.209 | 0.044 | **0.785** | 0.817 | 1.343 | 1,358 |

*(상관은 추정 Time-Lag 지점의 값 = 상류 신호의 **최선값**. 리포트 "정직한 한계" 문구도 이 수치를 인용한다.)*

**2026-07-28 기준 (직전, 비교용):** 기존 3h/+0.110/R² 0.012, 신설 10h/+0.156/R² 0.024.
→ 데이터가 늘며 상관이 다소 올라(0.11→0.25, 0.16→0.21) **신설은 처음으로 persist 를 넘어섰다**
(0.785 < 0.817). 다만 개선폭이 4%에 그쳐 리포트는 여전히 '사실상 동급'으로 표기한다.

- **정직한 한계(천장)**: 상류(광산품위+인출지점)가 야드 CaO 변동의 **4~6%만 설명**(07-28 기준 1~2%에서 상승). 목표(±0.5) 제어에는 여전히 크게 부족.
- **상류 피처는 예측을 개선하지 못함**: AR+상류 MAE가 AR 단독보다 오히려 나쁨(07-28 기준 1.064>1.035, 0.796>0.790)
  → 운영 모델은 `use_upstream=False`(AR 전용)가 타당함이 재확인됨.
- **결론 불변**: 페어링 정정으로 수치는 바뀌었으나 **"상류 신호가 약해 처방적 배합제어는 시기상조"**라는 기존 진단은 오히려 강화됨.
- **원인 진단**: ① 야드 시간의 **25%만** OSP 인출기록 존재(버퍼/직송 미확인) ② 야드 CaO 지속성 큼(자기상관 1h≈0.73) → 상류보다 야드 내부동역학이 지배 ③ 지점→품위가 일단위·75% 커버리지로 거침.
- **신호 강화에 필요한 것(사용자/데이터)**: OSP↔야드 버퍼/직송 여부, 야드 추가 투입원, 인출지점 실측 품위.

- 구현: `src/data/clean.py`(정제) + `src/matching/pipeline.py`(매칭). 스크립트 `scripts/build_matched_dataset.py`.
- 산출: `data/processed/matched_hourly.csv` (148 시간행 × 14열), OSP 예상CaO 매칭 커버리지 100%(라인 433/전역 22).
- **한계(정직 보고)**: OSP 예상CaO ↔ 야드 CaO 상관 0.17 (약함). 원인 가설:
  1. **정적 구역-품위 테이블** — 구역(파일)은 시간에 따라 다른 품위로 재적재되는데 6주 평균을 써 시간정보 소실. → **시간인지(most-recent-loading) 재고 모델**로 개선 필요.
  2. Time-Lag가 상수 아닐 가능성(물량 의존).
  3. 야드 시간내 변동이 큼(시간 집계로 소실).
- **다음 개선**: 구역별 '최근 적재 품위'를 인출시각 기준으로 매칭(재고 추적) → 상관/예측력 향상 기대.

## 5. 목표 & 제약 + ⭐️핵심 발견

- **목표**: 야드 적재 CaO 평균 44.6%, std ≤ 0.5
- **CaO 실측 통계**:
  | 소스 | n | 평균 | std | min | max |
  |------|---|------|-----|-----|-----|
  | 49Q광산 | 141 | 46.09 | 2.96 | 38.7 | 51.9 |
  | 47Q광산 | 59 | 45.56 | 0.46 | 43.9 | 46.7 |
  | **CNA야드(최종)** | 20,952 | **44.67** | **1.85** | 25.0 | 54.2 |
  | 45Q야드 | 3,839 | 45.40 | 2.26 | 38.4 | 53.0 |
- ⭐️ **야드 CNA 평균(44.67)은 이미 목표(44.6)에 근접 — 진짜 문제는 '분산(std 1.85)'을 0.5로 줄이는 것.**
  → ML 목표는 평균 이동이 아니라 **변동성 제어(안정화)** 에 초점.
- CNA min 25.0 = 센서 드롭아웃/이상치 가능성 → 검증 게이트 점검 대상.

## 6. 데이터 품질 이슈 (정제 TODO)
1. 49Q `이송물량(톤)` object형 + '-'(운휴) → numeric 변환
2. OSP적재구역 복합코드(예: "50/45/50/45/55") 파싱/전개 규칙 필요
3. OSP신설 `공정구분` 전부 NaN → '신설' 보정
4. 시각 컬럼(문자열) → time 파싱, 일자와 결합해 datetime 생성
5. 야드 `Yard` 컬럼 전부 결측 → 제거 검토
6. CNA 이상 저품위(25.0 등) 처리 방침 필요
