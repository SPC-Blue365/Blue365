# 핸드오프 상태 (Handoff State)

> ⭐️ 새 세션 시작 시 Main-Agent가 **가장 먼저** 읽는 파일입니다. (CLAUDE.md §0 / §5)
> 주요 단계 완료 시, 또는 사용자가 "handoff / 핸드오프 / 저장해줘" 요청 시 갱신합니다.

- **최종 업데이트**: 2026-08-14 (배합 처방서 완성 · **2차 현장 문의 작성 완료 — 발송/회신 대기**)
- **현재 파이프라인 단계**: `5. 운영·발표 준비 완료` (추적·예측·모니터링·통합리포트)
- **현재 활성 Agent**: `[Main-Agent]`
- **데이터 보관 정책**: 🔒 **로컬 전용** — GitHub 커밋 금지 (`.gitignore`), 필요 시 `BLUE365_DATA_DIR` 환경변수
- **최신 커밋**: `033f19e` 배합 처방서 완성 · 구역 귀속 붕괴 발견

> ⚠️ **디자인 방향 관련 기록** (2026-07-31): `canvas-design` 스킬로 만든 시각 체계
> (SEDIMENT INDEX — 석회색 종이 + 3잉크 팔레트 플레이트·리포트 재조판)는
> **사용자가 "디자인이 별로다"라고 판단하여 전량 철회**했다. 관련 커밋 3개
> (플레이트 PNG/PDF·플레이트 HTML·리포트 재조판)를 `git reset --hard eb5320d` +
> force-push 로 이력에서 제거했다.
> **다시 시도하지 말 것** — 리포트는 현재의 남색(#12395c) 기반 디자인을 유지한다.
> 디자인 변경은 사용자가 명시적으로 요청할 때만 착수한다.

> 📌 **공정 구조 (2026-08-05 사용자 확정) — 분석의 전제**
> ```
> 광산 채굴 ─┬─→ [수항: 사일로 10,000톤, ~1,500 t/h] ─→ G/C ─┐
>            └──────────────────────────→ H/C ──────────────┴─→ (OSP행 벨트 1개) → OSP
> ```
> · **G/C**=자이레토리 1,500~1,600 t/h · **H/C**=함마 1,000~1,100 t/h · **G/C+H/C**=1,500~1,600 t/h
> · 동시 가동이 **가산되지 않는 이유 = OSP행 벨트가 1개**라 거기서 만나기 때문(물리적 근거 확인).
>   배합 최적화 제약식도 단순 합산 금지.
> · ⭐️ **수항은 버퍼** → 채굴 품위와 OSP 품위 사이에 **지연·혼합** 발생. 상류-하류 상관이
>   0.21~0.25 로 낮은 것의 물리적 설명 후보다.
> · 데이터 파일은 **`data_v2.xlsx`** (49Q 에 `C/R(생산방법)`·`비고` 컬럼 추가). `S.DATA_FILE` 참조.
> 상세: `docs/data_schema.md` §6-0-8 (능력) · §6-0-9 (수항)


> 📦 **산출물 3종 — 역할이 다르다 (2026-08-14 확정)**
> | 파일 | 역할 | 생성 |
> |---|---|---|
> | **`outputs/prescription.html`** | ⭐️ **최종 산출물** — "몇 번에서 몇 톤, 몇 시간" 처방 + 적중 확률 | `scripts/make_prescription.py` |
> | `outputs/brief.html` | 현황 한 장 (목표 대비 어디까지 왔나) | `scripts/make_brief.py` |
> | `outputs/final_report.html` | 근거·검증 전체 (7탭, 5.6MB) | `scripts/run_all.py` |
> · 사용자 지적: **탭이 많고 내용이 방대하면 한눈에 안 들어온다.** 공유 대상은 관리자다.
>   새 산출물은 **의도에 답하는 것만** 담고, 근거는 `final_report.html` 로 링크한다.
> · 스타일은 `src/visualization/webstyle.py` 공용 토큰을 쓴다.

---

## 🔖 복원 지점 — CHECKPOINT ③ (최신): "임원 리포트 + 코드베이스 정리 완료" (2026-07-25)
> **"아까 저장해 놓은 지점으로 돌아가줘"** → 이 지점으로 복귀.

**이 지점의 상태:**
- **임원용 통합 리포트** `outputs/final_report.html` (7탭: 개요·추적흐름·변경일자별추이·예측모델·관리도·모니터경보·배합최적화)
  · 경영요약(신호등·KPI·핵심진단·실행계획) + 그래프별 한줄 캡션 + 용어집
  · **기간 직접설정**(년-월-일) → KPI·핵심진단 자동 재계산 / 그래프별 기간버튼(1주·2주·1개월·1년·전체)+슬라이더
  · 야드변경 타임라인(Gantt, CaO/MgO 토글), Sankey 2종, 표준편차 3종(야드별·라인별·연속측정)
- **코드베이스 정리**: 중복 스크립트 3개·미사용 로더/스펙·미사용 의존성 5개·템플릿 잔재 제거
- **검증 게이트 실제 연결**(`dataset.load_sources` → 매칭 전 자동 실행, 현재 ERROR 0)
- **테스트 40개** (정제·매칭 핵심 로직 18개 추가), **`scripts/run_all.py` 원클릭 실행**

---

## 🔖 복원 지점 — CHECKPOINT ②: "운영 대시보드 + 야드변경 추적" (2026-07-24)
> 사용자가 **"아까 저장해 놓은 지점으로 돌아가줘"** 라고 하면 **이 최신 체크포인트**로 복귀한다. (①은 이전 단계)

**이 지점의 상태 = 아래 전부 완료·커밋됨:**
- 추적(정제→매칭→Time-Lag) + 예측(Ridge AR, 10모델 벤치마크로 확정) + 배합최적화 스캐폴드.
- **운영 대시보드 `streamlit_app.py` 4탭**: 실시간 모니터·추적흐름(Sankey)·관리도(제어차트)·경보이력.
- **야드변경 추적**: 신규 시트(기존/신설라인 야드변경) → 라인→야드(Y1/Y2) Sankey + **CaO/MgO 버튼 토글** + 변경일자별 추이. 차트 날짜 `2026/07/24` 형식.
- 경보 엔진(설정가능)·경보 이력 로그(CSV). 테스트 22개 통과.

**이 지점에서 재개할 후보 작업:**
1. 배합 최적화(경로2) 정밀화 — 데이터 축적 시.
2. 데이터 축적 시 `scripts/benchmark_models.py` 재실행 → 최적 모델 재추천.
3. (보류) 이메일 알림 — 사내 SMTP·기밀 정책 확인 후.

**⚠️ 재개 시 필수**: 원본 데이터는 로컬 전용이라 **새 세션에선 `data/raw/data_v2.xlsx`(야드변경·OSP재고 포함 **9시트**, 49Q 에 C/R·비고)를 다시 첨부/배치**해야 한다. 이후 **`python scripts/run_all.py`** 한 번으로 전체 재현(검증→매칭→학습→모니터→통합리포트), 대시보드는 `streamlit run streamlit_app.py`.

---

## ✅ 확정 사항 (2026-07-23)
- **운영 예측 모델 = Ridge (정규화 선형, AR 피처)** 로 확정. (10개 모델 벤치마크 결과, 선형 계열 최적)
- **모델 거버넌스(재벤치마크) 프로토콜**: 모델 선택은 데이터 주도이며 고정이 아니다.
  - **트리거**: 데이터가 크게 늘 때(예: 행수 2배↑ 또는 분기 1회) 또는 사용자 요청 시.
  - **실행**: `python scripts/benchmark_models.py` → 10개 모델 재검증 → 최적 재추천.
  - 데이터가 많아지면 부스팅(XGBoost/LightGBM)이 선형을 앞설 수 있으므로 그때 재평가해 교체 판단.
  - 사용자가 **"데이터 쌓였으니 모델 다시 추천해줘"** 라고 하면 이 절차를 수행한다.

## 🔖 복원 지점 — CHECKPOINT ①: "파일럿 예측모델 완성" (2026-07-23)
> 사용자가 **"아까 저장해 놓은 지점으로 돌아가줘"** 라고 하면 **이 체크포인트**로 복귀해 이어서 진행한다.

**이 지점의 상태 = 아래 전부 완료·커밋됨:**
- 추적 파이프라인(정제→매칭→Time-Lag) + 라인별 하이브리드 예측(Ridge AR) + 배합최적화 스캐폴드(scipy) + 파일럿 HTML 리포트.
- 방침 확정: **경로1(예측·모니터링)=메인**, **경로2(배합최적화)=향후 데이터 축적 시 정밀화(항상 병행 유지)**.

**이 지점에서 재개할 다음 작업 (사용자 요청):**
1. **최적 모델 탐색(메인)**: 여러 알고리즘(XGBoost/LightGBM/RandomForest/ElasticNet 등) 비교 + 하이퍼파라미터 튜닝, 시계열 CV로 최적 예측모델 선정. (`src/models/`)
2. 예측·모니터링 운영 도구화(경보 임계·대시보드).
3. 경로2는 데이터 축적 대비 인터페이스 유지.

**재개 방법**: 데이터 `data/raw/data_v2.xlsx` 로컬 배치 후 → `python scripts/run_all.py` 로 현 상태 재현 가능.

---

## ⭐️ 현재 상태 (2026-07-23 야간) — 하이브리드 예측 모델 완성, 정직한 성능
도메인 정정 반영: **기존/신설 = 서로 다른 야드(4-5K/6-7K 킬른)**, **P/W=인출지점**, 품위=시간인지 매칭.
**야드 페어링(사용자 확정): 기존↔45Q, 신설↔CNA.**

**예측 모델 최종(Ridge AR, 시계열 CV) — `src/models/forecast.py`:**
| 라인→야드 | MAE | R² | (persist / naive) |
|---|---|---|---|
| 신설→CNA (6-7K) | **0.80** | +0.51 | 0.83 / 1.29 |
| 기존→45Q (4-5K) | **1.04** | +0.54 | 1.02 / 1.77 |
- 야드 CaO **지속성(AR)** 이 예측력의 핵심. **상류(OSP) 피처는 예측 개선 없음**(상관 0.18) → 처방적 배합제어는 현 데이터로 약함.
- 목표 MAE<0.5 **미달**(달성불가 정직고지): 상류 제어정보 부족이 원인. 단, naive 대비 대폭 개선 → **실시간 모니터링·조기경보용으로 실무 유용**.
- **다음 결정(사용자)**: 예측모델을 운영도구(모니터링/경보)로 확정할지, 배합최적화(scipy) 프레임워크를 약한신호 기반이라도 구축할지.

## 1. 완료 작업 요약 (Done)

### 2026-08-14 (3) — ⭐️배합 처방서 완성 · 구역 귀속 붕괴 발견
- 사용자 의도를 그대로 내는 **최종 형태** 구축: "OSP 몇 번에서 몇 톤, 몇 시간 뽑으면 기준에 드는가".
  · `src/matching/inventory.py` (구역 재고 복원 — 라인 실사에 anchor)
  · `src/optimization/prescribe.py` (처방 + **규격 적중 확률**)
  · `scripts/make_prescription.py` → `outputs/prescription.html` (18KB)
  · `src/visualization/webstyle.py` (브리프·처방서 공통 스타일)
- **실행 결과**: 기존 95번 339t+20번 165t → CaO 44.60% (적중 **43%**) /
  신설 5개 구역 6,105t → 45.12% (적중 **47%**, 저품위 재고 부족으로 목표 미달).
  → **LP 가 목표를 맞춰도 실제 적중은 동전 던지기.** 운전 사용 기준은 80%+ 로 잡았다.
- **⭐️⭐️ 근본 원인 발견 — 구역 귀속 붕괴**: 구역별 적재 vs 인출이 기존 **11/17 구역(인출의 55%),
  최악 21배** 어긋난다. 패턴은 **적재는 95·100번, 인출은 60~90번**.
  → **인출 기록의 구역 번호가 적재 위치를 가리키지 않는다.** Q1·Q2·Q4 답변과 전부 정합하며,
    **구역 단위 상관이 0.2 를 못 넘던 이유**다. `prescribe.zone_balance_health` 로 상시 감시.
- **대안 검토(보류)**: 라인 전체를 하나의 더미로 보는 완전혼합 모델. 기존 +0.211 vs 더미 +0.148,
  신설 +0.191 vs 더미 **+0.245** — 라인마다 우열이 갈리고 둘 다 설명력 4~6% 라 교체 근거 부족.
- **새 요청 항목**: **인출 기록에 '어디서 쌓은 물량인지' 병기** — 설비 없이 가능하고
  위 붕괴의 상당 부분을 해소한다.
- 테스트 **142개** 통과(`tests/test_prescribe.py` 13건 신규). 상세 §6-0-16.

### 2026-08-14 (2) — 현장 답변 Q1·Q2·Q4 반영 · 톤가중 버그 수정
- **질문 5건 전부 회신 완료.** Q3·Q5 는 앞 절, 여기서는 Q1·Q2·Q4.
- **Q1 — 구역별 측정은 하지 않음. 6/10 재고 기존 36,000 · 신설 30,000톤(라인 합계만).**
  · ⭐️ **요청 철회**: "구역별 기초 재고·품위로 미매칭 13.1% 해소"는 **원리적으로 불가능**하다.
    추정분은 **메우는 대신 `grade_source` 로 표시**하는 것으로 확정.
  · ✅ 제시값이 실사 기록 6/10 마지막 값과 **정확히 일치** → 라인 단위 재고는 신뢰 가능.
- **Q2 — 구역은 '칸'이고 칸 높이 넘으면 옆 칸으로 흘러듦. 잔량은 추정.**
  · ⭐️ **매칭 전제를 건드린다** — 구역 라벨은 엄밀한 경계가 아니라 근사다.
  · 기존 35번 구역(적재 기록 없이 1,250톤 인출) 미스터리 **해소**.
  · **⚠️ 인접 구역 스무딩 시도 → 기각.** 처음엔 상관이 +0.231→+0.354 로 좋아 보였으나
    **lag 재탐색으로 표본이 줄며(n 60→32) 다른 lag 가 뽑힌 착시**였다. lag·표본을 고정해
    짝 부트스트랩하면 Δr 이 **음수**(기존 −0.140, 신설 −0.084, 둘 다 CI 가 0 포함).
    → 채택 안 함. 넘침이 **과적재 시 국소적**으로 일어나기 때문으로 보인다.
  · **결론**: 구역 단위 매칭 정밀도에 **물리적 상한**이 있다 → 인출 지점 실측 근거 강화.
- **Q4 — 90·100번은 47Q 물량(80%+), 인출 후 90번 이전을 만실 47,000톤까지 채움.**
  · ✅ **데이터가 강하게 뒷받침**: 90번 이상 적재가 47Q 는 **73.4%**, 49Q 는 **1.0%** (73배 편중).
  · ✅ **만실 47,000톤 교차검증**: 실사 재고 최고치 기존 45,000 · 신설 43,000 — **한 번도 안 넘음**.
  · 인출 규모는 회당 1,500톤이나 **연속 캠페인으로 묶으면 13,600~18,200톤**으로 언급과 일치.
  · 구역 100 은 인출 기록 없음(적재만).
- **[🐞 수정] 구역-일별 품위가 단순평균 → 톤가중으로 교정.** Q4 검증 중 발견.
  구역 90 에 49Q(1,299t, 49.85%)와 47Q(7,820t, 45.71%)가 함께 있는데 1:1 로 섞고 있었다.
  · 영향: 구역-일 296건 중 26건(8.8%)이 0.3%p 이상, 최대 1.61%p.
  · 하류 상관 변화는 노이즈 범위이나 **정확성 문제**라 교정. 품위 수지는 개선
    (신설 기말 함의 CaO 41.84 → **43.57%**).
- **신규 스키마 상수**: `OSP_CAPACITY_TON=47,000` · `OSP_OPENING_STOCK` · `OSP_ZONE_STOCK_MEASURED=False`
  · `OSP_ZONE_SPILLOVER=True` · `OSP_47Q_ZONES=(90,95,100)`
- 브리프 갱신: 불가능한 요청을 빼고 **‘월마감 계량 조정값 공유’**(설비 없이 바로 정확해짐)로 교체.
- 테스트 **129개** 통과. 상세 `docs/data_schema.md` §6-0-15.

### 2026-08-14 — 현장 답변(Q3·Q5) 반영 · 한 장짜리 브리프 신설
- **데이터 재업로드 확인**: `data_v2_260813.xlsx` 재업로드본은 **내용이 완전히 동일**하다.
  9시트 전부 행수·셀값 일치했고, zip 엔트리 차이는 **스크롤 위치·선택 셀·활성 탭·수정시각**뿐
  (`xl/worksheets/sheet1·3·4·9.xml`, `docProps/core.xml`). **엑셀을 열었다 저장만 한 파일**이다.
  → 파이프라인 결과 불변. 최신본으로 교체만 하고 재분석은 생략(§2-1 정직 보고).
- **✅ Q3 답변 — 인출 계량은 시설 3호 벨트스케일, 월마감 때 공장이 증량·감량을 정함.
  광산·공장 스케일 수치는 서로 다름.** ⭐️ 해석이 바뀐다 — 종전 '원인 미상 −58,943톤(4.5%) 결손'은
  **손실이 아니라 두 계량 기준의 차이**이며, **0이 아닌 것이 정상**이다.
  · 검증: β(=인출계량÷적재계량)가 **두 라인 모두 6→7월에 하락**(기존 1.044→0.869,
    신설 1.123→1.040). 공장 일괄 조정이라는 설명과 정합. (월 경계 관측이 1개뿐이라 정황 일치)
  · **β 는 월 단위로 따로 추정해야 한다** — 여러 달 뭉친 평균은 무의미.
- **✅ Q5 답변 — OSP 재고는 49광구 CCP 산정, 기준은 있으나 담당자별 오차가 있는 개략치.**
  → 재고를 기준선으로 쓰는 검사(기말 함의 품위·β)는 이 오차를 물려받는다. 신설은 지렛대 41배라
    재고 1,000톤 오차 = 함의 품위 약 1.8%p. **재고 잔차를 계량 드리프트 증거로 단정 금지.**
- **[신규] `scripts/make_brief.py` → `outputs/brief.html`** — 사용자 지적("UI·구조가 너무 방대해
  보기 싫다")에 대한 **별도 결과물**. 기존 `final_report.html` 은 **그대로 유지**한다.
  · 탭 없음 · 한 번 스크롤 · 섹션마다 **질문 → 한 줄 답 → 최소 근거**
  · 차트를 Plotly 대신 **인라인 SVG** 로 생성 → **5,660KB → 21KB (270배)**
  · 라이트/다크 양 테마 · 420px 폭까지 가로스크롤 0 · JS 오류 0
  · 수치는 전부 실데이터에서 계산(하드코딩 없음), 근거 경로를 각주로 명시
- 상세: `docs/data_schema.md` §6-0-14.

### 2026-08-13 — 교차인출 ⓐ 확정 · 품위 출처 플래그 · '5월 재고' 가설 검증
- **✅ 교차인출 귀속 확정 (사용자)**: 해석 **ⓐ = 기록된 시트가 곧 그 라인의 인출**. 비고의
  화살표는 목적지 메모. 현행 구현이 이미 ⓐ라 **코드 변경 없음**, Open Issue 종결.
- **[신규] `grade_source` 플래그** — 부여된 품위가 **실측 기반(`zone_history`)인지 대체값
  (`line_mean`/`no_zone`)인지** 구별한다. 대체값을 채우기 **전에** 기록해야 구별이 가능하다.
  → **인출의 13.1%(187,150톤)가 대체값**임이 드러났다(기존 20.7% · 신설 11.0%).
  리포트 「품위 수지 검증」 섹션에 명시.
- **❓ 사용자 가설 검증 — "5월 잔여 재고 = 매칭 안 되는 물량"**: 네 갈래로 측정했다.
  · **물량 수지: 기각.** 기초재고 s0 는 이미 수지식에 있어, 키우면 결손이 **오히려 커진다.**
    신설만 s0+60,000t 에서 닫히지만 **같은 가정이 기존을 +9,114→+69,114t 로 7.6배 악화**시킨다.
    결정적으로 **실사 재고 추세가 평평**(기존 +1·신설 −3 톤/일)해 59,000톤 감소가 없었음이 반증된다.
  · **체류시간**: 신설 기초재고는 **1.6일**, 기존은 **8.2일**이면 소진 — 영향 기간이 원리적으로 짧다.
  · **현상 자체는 실재**: 미매칭 물량이 주1 52~100% → 주6 3~10% 로 **감소하는 패턴**은
    사용자님 직관과 일치. 다만 규모(187,150t)가 기초재고(67,000t)의 2.8배, 지속(5주)이
    체류시간의 5~20배 → 원인은 **구역별 적재 이력의 뒤늦은 시작**(zone coverage ramp-up)이다.
  · **예측력: 개선 없음.** 미매칭 제외 시 상관 기존 +0.231→+0.249, 신설 +0.192→+0.201.
    **표준오차(±0.13 / ±0.067)의 1/7 수준**이라 통계적으로 무의미.
  → **가치는 예측력이 아니라 정직성.** 진짜 개선하려면 **구역별 기초 재고량·품위**가 필요하다.
- 테스트 **127개** 통과(`grade_source` 회귀 1건 추가). 상세 `docs/data_schema.md` §6-0-13.

### 2026-08-13 — 데이터 갱신 #5 (`data_v2_260813.xlsx`) · MgO 인출 매칭 · 품위 수지 검증
- **구조**: 9시트 동일, **순수 확장**. 기간 — 49Q~08/10(267행) · 47Q~08/10(100행) ·
  OSP인출_기존~08/10(106행) · OSP인출_신설~08/13(305행) · CNA~08/13(33,019행) ·
  신설라인 야드변경~08/13(32행) · OSP재고~08/13(실사 5,124건).
  45Q 감마레이(3,839행)·기존라인 야드변경(9건)은 **미갱신**(기존 라인 07/21~ 미가동과 정합).
- **겹치는 행 diff**: 실제 값 변경은 **단 1건** — 49Q 223행 CaO 45.348→42.12, MgO 2.592→2.89.
  나머지 차이는 전부 날짜/시각 직렬화 형식 변화(`'11:39'`→`'11:39:00'` 등)로 의미 없음.
- **[🐞 기능 공백 — 수정] 인출에 MgO 가 아예 부여되지 않았다.** `assign_expected_cao_timeaware()`
  가 CaO 만 부여해 `expected_mgo` 컬럼이 없었다 → MgO 는 재고 흐름과 대조할 방법이 없었다.
  `pipeline.GRADE_COLS` 로 일반화해 **두 성분을 같은 asof 매칭 한 번에서 함께** 뽑는다
  (따로 매칭하면 서로 다른 광산 행이 섞여 실재하지 않는 성분 조합이 된다).
  집계에도 `osp_expected_mgo`(톤가중) 추가. → **CaO·MgO 부여 커버리지 모두 100%**.
- **[신규] `src/matching/balance.py`** — 물량뿐 아니라 **성분량까지 닫는** 품위 수지 검증.
  설계상 반드시 지킨 두 가지: ① 물량 결손을 **양방향**으로 먼저 보정(한쪽만 보정하면
  반대 부호에서 오판 — 실제로 1차 시도에서 기존 라인이 거짓 ❌ 로 나왔다) ②
  **민감도(처리량÷기말재고)를 함께 보고**(신설 41배 → 물량 1% 오차가 함의 품위를 19%p 흔든다).
- **검증 결과**: 결손 보정 후 **네 값 모두 물리적 범위 내** — 기존 CaO 49.98·MgO 1.95,
  신설 CaO 41.84·MgO 4.55. 변동폭도 63~92% 보존. → **품위 매칭 자체는 성립**한다.
- **⭐️ 남은 문제는 계량이다**: 라인 귀속을 지우고 **합산해도** 적재 1,323,057t vs 인출 1,386,000t
  으로 **−58,943t(4.5%)** 이 남는다(β=1.048). 합쳐도 남는다는 것은 원인이 **교차인출이 아님**을 뜻한다.
- **⚠️ 발견**: 기존 라인의 CaO~MgO 상관이 기준선 −0.159 → 부여 후 **+0.088 로 부호 반전**.
  → **MgO 는 수지 검증용으로만 쓰고 예측 피처로는 쓰지 않는다.**
- 리포트에 **「품위 수지 검증」 섹션** 신설(어디서 왔나 탭). 테스트 **126개** 통과(신규 11건).
- 상세: `docs/data_schema.md` §6-0-12.

### 2026-08-05 — 예측 탭 정합 수정 · 배합 로드맵 준비도 알람
- **복원 지점**: `restore/2026-08-05-before-bugfix` 브랜치(= `fc872d9`) — 사용자 요청으로 저장.
- **[🐞 수정] 같은 지표가 두 값으로 표시**: 성적표 평균오차(뒤 30% holdout)와 벤치마크 표(시계열 CV)가
  기준이 달라 기존 0.91 vs 1.035 로 어긋났다. **공식 성적은 벤치마크**임을 캡션에 명시하고,
  성적표는 '어떻게 틀렸는지 보는 용도'로 자리매김. **추천 모델(LinearRegression)과 확정 모델(Ridge)**이
  달라 혼란스럽던 것도 "사실상 동률 → 정규화가 있는 쪽 유지"를 표에 함께 표기해 해소.
- **[🐞 수정] 적중률과 평균오차가 모순처럼 보임**: 평균 0.91인데 ±0.5 적중률 37%. 버그가 아니라
  **오차 분포가 치우쳐(평균−중앙값 +0.24) 평균이 큰 실수 몇 건에 끌려간 것**. KPI 에 **오차 중앙값**을
  함께 표시하고("절반은 0.67보다 잘 맞혔습니다") 캡션에 이유를 적었다.
- **[신규] `src/models/roadmap.py`** — "배합 최적화까지 데이터가 얼마나 더?" 에 데이터로 답한다.
  요건을 **성격이 다른 두 갈래**로 나눠 보고한다(섞으면 "조금만 더 모으면 되겠네"로 오독된다):
  · **갈래 A 검증 사례 — 시간이 해결**: 야드변경 구간 **37/130건(28%)**, 월 18건 → **약 5.2개월** 남음.
  · **갈래 B 구역 품위 정밀도 — 시간이 해결 못함**: 주요 구역 13개 평균 오차 **±0.56%p** >
    허용폭 ±0.5%p. 표본으로 ±0.1%p 까지 좁히려면 구역당 **823건**(현재 22건, **37배**) → 수 년.
    원인은 **구역 내 산포(2~4%p)** 라 표본을 늘려도 '평균'만 정밀해진다 →
    **인출 지점 실측(분석기)** 같은 새 계측이 훨씬 빠른 길(설비 투자 판단 사항).
  · 학습곡선 검증: MAE 0.5 도달을 로그 외삽하면 **32만행(≈36년치)** — 데이터 양으로는 불가.
- **[알람]** `scripts/monitor.py` 가 실행될 때마다 준비도를 자동 판정하고, 충족되면
  `🎉 배합 최적화 착수 조건 충족` 배너를 띄운다(세션이 휘발성이라 상시 감시는 불가 → 실행 시 판정).
  리포트 '섞기 계획' 탭에도 진행률 막대와 두 갈래 설명을 넣었다.
- 검증: 테스트 **116개** 통과(회귀 2건 추가, 빈 입력 크래시 1건 수정) · 7탭 × 3해상도 겹침 0.

### 2026-08-05 — 데이터 갱신 #4 (`data_v2.xlsx`) · 공정 구조 확정
- **구조**: 9시트 동일, **행 변화 0**. 49Q 에 **`C/R(생산방법)`·`비고`** 두 컬럼만 추가된 순수 확장.
  → `S.DATA_FILE = "data_v2.xlsx"` 로 전환. 모델·지표는 전부 불변(기존 CV MAE 1.035 · 신설 0.785).
- **[공정 구조 확정]** 광산→OSP 경로 2가지(수항→G/C / H/C 직송)와 **OSP행 벨트 1개** 병목 확인.
  · 이전에 "동시 가동이 가산되지 않으니 하류 병목이 있을 것"이라고 추정했던 것의 **물리적 근거가 확인**됨.
  · 수항 = 사일로 **10,000톤**, 적재 **~1,500 t/h** (`S.SURGE_BIN_CAPACITY_TON` · `SURGE_BIN_FILL_TPH`).
- **⭐️ [해소] 직전 갱신의 '품위 삭제 18칸'은 삭제가 아니었다**: v2 비고를 보니 그 값들이
  `(수항채움 CaO, MgO, 톤)` 으로 옮겨져 있었다(5개 행 값 정확히 일치). **OSP 가 아니라 수항으로 간 물량**이라
  이송물량·품위를 비운 것이 맞다 — **기록 정정이 아니라 경로 구분**. §6-0-6 의 우려 해소.
- **[능력 상한 감사 — 초과 0건]** `dataset.capacity_audit()` 신설, 검증 게이트에 연결(초과 시 ERROR).
  · G/C 84교대 최대 **1,588**/상한 1,600 · G/C+H/C 35교대 최대 1,341/1,600 · H/C 16교대 최대 800/1,100
  · **주어진 능력치가 실측과 완전히 정합.** 동시 가동 최대가 G/C 단독보다 낮아 비가산성과도 부합.
- **[비고 파싱]** `clean.parse_surge_fill` · `parse_note_events` · `dataset.load_surge_fills` 신설.
  수항 적재 15건 **26,960톤**(07/15~07/25, 단일 최대 3,600톤 — 용량 10,000톤 내 정합),
  수항재고부족 3건 · 수항 단독 2건 · OSP 만실 5건 · 야드 만실 5건.
- **[비고 시각 파싱]** (사용자 지시로 이어서 진행) `clean.parse_note_window` · `parse_zone_windows`
  · 비고에 **교대 전체 생산구간**과 **구역별 부분 적치구간**이 같은 표기로 섞여 있어, **물리적 가능성**
    (구간 ≤ 8h, 물량÷시간 ≤ 능력상한)으로 채택 여부를 가른다. `24시`=자정(1440분) 처리.
  · **20개 교대에서 실제 생산구간 확보** → 가동시간 중앙 **5.11h(8h의 64%)**, t/h 중앙 837 → **1,480**.
    ⭐️ **8시간 가정은 t/h 를 크게 과소평가**한다 — 실제로는 능력 상한(1,600) 근처에서 돌다 멈추는 패턴.
  · 구역 창은 3건뿐이라 파서만 마련(Time-Lag 미반영).
- **[사용자 확정 3건]** ① 수항채움 행의 `이송물량`은 **OSP 몫만** → 총 채굴 = 이송물량 + 수항채움
  ② 07/20·07/25 의 `(49.25, 0.79, 1200톤)` 동일 값은 **붙여넣기 오류** → 서로 다른 날짜에 (CaO,MgO,톤)이
     같으면 `suspect=True` 로 자동 표시(지우지 않고 품위 통계에서만 제외) ③ **수항 인출 기록은 없다**
     → 수항 재고 곡선은 `누적 채움 − 누적 G/C 생산` 으로 역산해야 하며 동시가동 교대는 구간 추정만 가능(미구현).
- **검증**: 테스트 **111개** 통과(회귀 6건 추가) · 파이프라인 전 구간 정상 · 리포트 7탭 겹침 0·렌더실패 0.

### 2026-08-05 — 데이터 갱신 #3 (`data_v1_260805.xlsx`)
- **구조**: 9시트 이름·순서·컬럼 **전부 동일**. 스키마 변경 없음.
- **기간 확장**: 07/28 → **08/05**. 49Q 183→223행(~08/03) · 47Q 78→90행(~08/03) · OSP인출 기존 99→103행(07/20→**08/02, 재개**) / 신설 217→255행(~08/03) · CNA 24,601→**28,907행**(~08/05) · 신설 야드변경 24→28건(~08/04).
  · **불변**: 45Q 감마레이 3,839행(~07/21) · 기존라인 야드변경 9건(~07/15) → **기존 라인 야드는 여전히 정지**(보수).
- **⚠️ 겹치는 행에도 수정이 있었다** (직전 갱신과 달리 순수 추가가 아님). 셀 단위로 분류해 보고:
  · **49Q**: 백필 3(구역) · **삭제 18**(CaO/MgO 값이 빈칸으로 — 행 148·154·160·166 등 9개 행) · 반올림 31 · **정정 18**(이송물량 4081→4018, 구역코드 재표기, CaO 일부)
  · **47Q**: **정정 50** — 행 71 이후(07월 하순)가 교대시간·채굴일자·물량·품위까지 **통째로 재입력**됨
  · **OSP인출_신설**: 공정구분 217행 백필('신설') — 무해
  · **OSP 재고**: 백필 70(07/30~08/05 신규 실사)
  · 삭제된 품위 18칸은 §6-1 규칙대로 **분모에서 빠져** 평균을 끌어내리지 않는다(빈칸=0 처리 아님).
- **[버그] 구역코드 쉼표 표기 미지원**: 갱신본에 `25~30, 65~40` 형태가 처음 등장 → 기존 구분자 `[/\-~]`로는 `30, 65`가 숫자 변환에 실패해 **구역이 조용히 사라졌다**. `ZONE_DELIMITERS`에 쉼표·공백 추가(2건 영향). 관측된 표기 전수를 `docs/data_schema.md` §6-0-4에 기록.
- **[버그] 식별 불가 β 를 계량 배율로 보고**: 기존 라인이 멈춘 창(07/21~07/31)에서 인출 2,300톤뿐인데 재고는 움직여 **β=6.98**이 나왔다. `calibration_windows`에 `MIN_WINDOW_TON=5,000` + `PLAUSIBLE=(0.7,1.3)` 가드를 넣어 `ok=False`로 표시하고, 그래프·판정에서 빼되 **제외 사유를 리포트에 명시**(§2-1, 조용히 지우지 않음).
- **[문구 정합]** 리포트에 박혀 있던 "8시트"·"상관 0.18" 고정값을 **원본에서 실제로 세고 계산**하도록 변경(갱신 때마다 낡던 문제). `LineData.lag_corr` 신설.
- **결과 변화**
  · 야드: 신설 n=28,907 평균 **44.80** std **1.88** 규격내 **22%** / 기존 n=3,839 평균 45.40 std 2.26 규격내 15%(불변)
  · Time-Lag·상관: 기존 3h **+0.245**(0.110↑) · 신설 10h→**13h** **+0.209**(0.156↑) — 설명력 천장 1~2% → **4~6%**
  · 모델: 신설 CV MAE 0.790→**0.785**(n 776→917), 기존 1.035(불변). **신설이 처음으로 persist(0.817)를 넘어섬** — 다만 개선폭 4%라 리포트는 여전히 '사실상 동급' 표기.
  · **재벤치마크(§3 거버넌스 트리거 충족, 데이터 +18%)**: 신설 LinearRegression 0.785138 vs Ridge 0.785201 — **차이 0.008%의 동률** → 정규화가 있는 **Ridge 유지**. 기존은 persist(1.023)가 여전히 모든 모델보다 우수 → 모델 고유 예측력 없음(정직 보고).
  · β 추이: 기존 4개 창 **0.855→1.038 단조 상승**(드리프트 판정 유지) · 신설 6개 창 0.810~0.973 진동
  · 운영 상태: 기존 ⏸️가동중지(15일 지연) · 신설 🔴경고(최신 CaO 46.73%)
- **검증**: 게이트 ERROR 1(기지의 OSP 1행 구역 결측, 물량 2,000톤은 보존) · WARNING 1(재고 중복 14행). 테스트 **98개** 통과(회귀 2개 추가). Playwright로 7탭 전수 렌더 — 차트 18개 렌더 실패 0 · 텍스트 겹침 0 · 가로스크롤 없음.

### 2026-07-29 ~ 07-31 — OSP 실사 재고 통합 · 교대 시각 · 계량 정합
- **[신규 데이터] OSP 실사 재고 시트** (`a22ce67`): `data_v1.xlsx`에 **`OSP 재고`** 시트 추가(2024~현재, 5,040행).
  · **사용자 확정 규칙**: `차수` 1=1차(08~16) · 2=2차(16~24) · 3=3차(00~08). 재고 실사는 **교대 중간**에 시행 — 1차 12:30 · 2차 20:30 · 3차 04:30 (`S.STOCK_TAKE_HOURS`). **OSP1=기존 라인 · OSP2=신설 라인** (`S.OSP_TO_LINE`).
  · **사용자 확정 의미**: 이 재고는 **49Q 물량만** 담긴 것이고(다른 채굴장 없음), **전량 야드로** 나가며, 숫자는 그 시각의 **실재고(절대량)**다.
  · **사용자 고지**: 실사는 **육안 측정**이라 오차가 있다 → 정합 로직이 이를 감안해야 한다.
  · 시트가 블록 2개(OSP1/OSP2)로 나뉜 비정형 배치라 `S.STOCK_BLOCKS`·`S.STOCK_HEADER_ROW`로 파싱(`clean.clean_osp_stock`). 검증 게이트에 재고 결측·중복 규칙 추가(현재 WARNING 2건: 미입력 2행·날짜+차수 중복 14행→마지막 채택).
- **[정합] 실사 재고 vs 흐름 계산** (`bb030f9`, `eb5320d`): 적재−인출 누적으로 만든 흐름 재고를 실사값과 대조(`dataset.stock_vs_flow`).
  · **최소제곱 보정** `dataset.calibrate_flow` — 실사 = α + β×흐름. 증분 회귀는 감쇠 편향(errors-in-variables)으로 α=0.28·β=0.25 같은 비현실값을 내므로 **타당범위 가드 `PLAUSIBLE=(0.7,1.3)`** 를 두고 벗어나면 채택하지 않는다.
  · **사용자 확정: 인출 계량 = 벨트스케일** → 오차가 물량에 비례하는 **곱셈형(β) 모델이 물리적으로 타당**함을 확인. `figures.calibration_drift`로 **10일 창 β 추이 차트** 추가.
  · **결과(정직 보고)**: 기존 **β=0.927±0.003** — 10일 창이 0.855→1.040으로 **단조 증가**하고 신뢰구간이 겹치지 않음 → **드리프트, 교정 대상**. 신설 **β=0.906±0.002** — 0.81~0.97 사이 진동(추세 없음).
  · 잔차가 남는 이유를 실사 오차 하나로 몰지 않고 **① 육안 실사 오차 ② 기록 정확도 ③ 계량 드리프트** 세 갈래로 병기해 보고한다.
- **[교대 시각 반영]** (`e9995c1`): 사용자 제공 교대 시각(1차 08~16 / 2차 16~24 / 3차 00~08)을 `S.SHIFT_HOURS`로 정본화, 광산 데이터의 **일 단위 한계 제거**(구간 귀속 시 최대 48% 오차 발생하던 문제).
  · **인수인계 30분**: 실측 결과 대표시각이 15분 이동하고 **33개 구간 중 귀속이 바뀌는 건 0건** → 사용자 지시("큰 차이 없으면 제외")대로 **제외**. 판단 근거를 `S.HANDOVER_APPLIED = False`에 주석으로 남김.
- **[재고 추이 시각화]** (`bbbd439`, `a22ce67`): `figures.inventory_trend`(적재−인출 누적 증감) + `figures.stock_trend`(실사 vs 흐름, 라인별 서브플롯). 리포트·대시보드 양쪽 반영.
- **[구간 선택 기능]** (`0c45f95`, `5cd1cc3`): 야드변경 구간 드롭다운을 Sankey 2종에 추가하고, **구간 선택 시 해당 라인의 흐름만** 남기도록 분리(사용자 지적 — 데이터는 원래 라인별로 구분돼 있었고 필터가 시간만 보고 있었음). 대시보드에도 동일 선택기 추가. 구간 정본 로직을 `src/matching/segments.py`로 분리.
- **[예측 탭 개편]** (`4593049`, `532526b`): 임원용으로 **성적표·판정 우선**, 기술 벤치마크는 참고로 후치. **허용폭 선택(±0.5/1.0/1.5/2.0)** 드롭다운 + 개선폭 5% 미만이면 **"사실상 동급"** 명시(MAE 0.81 vs persist 0.83 → 모델 고유 예측력이 거의 없음을 숨기지 않음).
- **[버그 수정 4건]**
  1. **구간 경계 어긋남**(`1eb1a6f`, 사용자 발견): 집계 키가 시(時) 단위여서 구간 끝이 다음 변경 이벤트를 포함(45,880톤 과다) → **분(分) 단위 키** + 끝 = 다음 변경 −1분.
  2. **관리도 x축이 최근 감시창만 표시**(`ba1b826`, 사용자 발견): 7/20부터만 그려짐 → `ld.yard_series` 사용으로 전체 기간 복원. KPI도 정정(기존 7%→**15%**, 신설 17%→**23%**).
  3. **구역코드 없는 행 통째로 폐기**(`3a885b7`): 물량이 사라지던 문제 — 49Q 10,337톤 · OSP 2,000톤 누락. 물량이 있으면 `zone=NaN`으로 **보존**하도록 수정 → 4개 소스 모두 원본과 100% 일치.
  4. **글자 겹침 5곳**(`f3d8aab`, 사용자 발견): 예측 제목↔범례, "규격" 라벨 y축 잘림, stock_trend 범례↔서브플롯 제목·버튼, 표준편차 목표선 주석↔막대, 관리도 "규격"↔"평균" 충돌 → 범례 재배치·주석 플롯 밖 이동·`layer="below"`·최소간격 강제. **자동 bbox 스캐너**로 900/1180/1400px + 대시보드에서 겹침 0 확인.
- **테스트 96개 전부 통과.**

### 2026-07-28 이전
- **[데이터 갱신 #2]** (2026-07-28 야간): 광산 49Q·47Q 갱신본 반영 (`data_v1_260728.xlsx` → `data/raw/data_v1.xlsx`).
  · **갱신 전 비교 검사**: 시트 8개 이름·순서·컬럼 **모두 동일**. 변경은 광산 2시트뿐 — 49Q 153→183행(+5일, 07/22→**07/27**), 47Q 71→78행(+6일, 07/21→**07/27**). OSP·CNA·45Q·야드변경 4시트는 행수·기간 **완전 동일**.
  · **무결성**: 기존 값이 다른 값으로 **변경·삭제된 셀 0개**. 49Q에서 **빈칸 18칸이 값으로 채워짐**(행 149·151~154) → 순수 추가 + 백필. 47Q는 순수 추가.
  · **검증 게이트**: ERROR 0 · WARNING 0 (광산 295행·OSP 599·야드 3839/24601·야드변경 33).
  · **결과 변화**: 광산 물량 +133,524톤(49Q +99,257, 47Q +34,267). 47Q CaO 결측률 15.0%→13.8%. Sankey 47Q→기존 CaO 45.61→45.69, 49Q→신설 45.97→46.08.
    **야드 데이터는 불변이므로 모델 성능도 불변**(기존 CV MAE 1.035 / 신설 0.790, n 141·776). 신설 Time-Lag만 10h→**6h**로 재추정(상류 상관 0.156→0.172).
  · **재벤치마크**(§3 모델 거버넌스): 신설 **Ridge 1위 유지**(0.790). 기존은 LinearRegression 1.032 vs Ridge 1.035로 사실상 동률이나 **persist 기준선(1.023)이 여전히 최상** → 기존 라인은 모델이 지속성을 못 넘음(정직 보고). **확정 모델 Ridge 유지.**
  · **관찰**: 기존 라인 채굴은 **07/24까지** 기록 후 중단(야드 45Q는 07/21 정지) — 보수 가동 중지와 정합적.
  · 산출물 재생성 완료(run_all), Playwright로 리포트 7탭·Sankey 호버·기간 재계산 검증. 테스트 63개 통과.
- **[중대 버그] 품위 빈칸을 0으로 평균에 포함** (2026-07-28, **사용자 발견**): 물류 개요 Sankey의 물량가중 평균이 품위 빈칸 행을 **분모(물량)에는 넣고 분자에는 0으로** 넣어, 평균이 0쪽으로 끌려 내려가고 있었다.
  · **영향 (47Q는 15%가 빈칸, 물량의 13.6%)**: 47Q→기존 CaO **36.63 → 45.61 (+8.98%p)**, 47Q→신설 CaO 41.02 → 45.49, MgO도 각 +0.89 / +0.45. 49Q는 빈칸 1행뿐이라 영향 미미(≤0.05).
  · **함께 발견**: 야드 일별집계(`yard_daily`)가 CaO/MgO에 **공통 건수**를 써서, MgO만 13.6% 결측인 45Q 야드의 MgO가 **2.94 → 3.40**으로 왜곡.
  · **원인 지점**: `figures.sankey_daily_aggregates._rows`의 `fillna(0)` + JS `_sumRows`의 `wc/ton`. **정제 단계(`clean.py`)는 정상** — 집계 단계만의 문제.
  · **수정**: 행 스키마를 `[날짜, 물량, 물량×CaO, **CaO유효물량**, 물량×MgO, **MgO유효물량**]`로 확장해 **성분별로 유효물량을 따로** 집계(CaO·MgO는 빈칸 위치가 다름). 야드 일별행도 `[날짜, CaO건수, CaO합, MgO건수, MgO합]`으로 분리. 공용 헬퍼 `figures._wavg()` 도입.
  · **부수 정정**: 정적 호버가 **비가중 평균**이라 JS 재계산(물량가중)과 값이 달랐던 불일치도 해소, 정적 호버에 MgO 추가. 야드변경 Sankey의 `np.average`(빈칸 1개에 링크 전체가 NaN이 되는 구조)도 `_wavg`로 통일.
  · **검증**: 원본 직접계산 = JS 집계 = 정적 호버 **3경로 12개 값 전부 일치**, Playwright로 초기 렌더·기간 재계산 후 호버값 확인. 회귀 테스트 3개 추가.
  · **전수 점검 결과**: `fillna(0)`이 평균에 섞이는 다른 지점 **없음**(나머지는 ML 피처 결측대체·시각 파싱용). OSP·CNA·야드변경은 결측 0%, 49Q는 1행, 45Q는 MgO만 결측.
- **[중대 버그] 야드 페어링 불일치** (2026-07-28): `scripts/build_matched_dataset.py`가 사용자 정정 **이전의 옛 페어링**(기존↔CNA, 신설↔45Q)을 자체 하드코딩하고 있어, `data/processed/matched_*.csv`가 **야드가 뒤바뀐 채** 생성되고 있었다.
  · **영향 범위**: 매칭 CSV 산출물과 거기 출력된 lag/상관 수치, 그리고 그 값을 인용한 `docs/data_schema.md` §7 표. **모델·리포트·대시보드는 `config/schema.YARD_PAIR`를 쓰므로 처음부터 정상**(CV MAE 1.035/0.790 불변으로 확인).
  · **수정**: 두 스크립트 모두 정본 `S.YARD_PAIR` 사용, 중복 정의 제거. 회귀 테스트 2개 추가(정본 일치 + 스크립트 하드코딩 금지).
  · **정정된 수치**(확정 페어링 재계산): 기존→45Q lag 3h 상관 **+0.110**(R² 0.012), 신설→CNA lag 10h 상관 **+0.156**(R² 0.024). 종전 문서값 0.34/0.39(R² 0.11/0.13)는 뒤바뀐 페어링 기준이었다.
  · **해석**: 상류 신호가 문서보다 **훨씬 약함**(설명력 11~13% → 1~2%). AR+상류가 AR 단독보다 MAE 나쁨 → `use_upstream=False` 타당성 재확인. **"처방적 배합제어 시기상조"라는 기존 결론은 강화됨.**
- **[품질/버그수정]** (2026-07-28): 기간 설정·가동 중지 라인 조합에서 터지던 **버그 9건 발견·수정** (엣지케이스 전수 점검).
  1. 정적 리포트가 광산·OSP를 기간 필터에 **미반영** → `filter_period` 적용(모든 탭 동일 기간).
  2. `filter_period`의 **종료일이 그 날 데이터를 제외**(`--end 2026-07-28` → 07/28 348행 누락) → **하루 끝까지 포함**으로 수정, 리포트 JS 필터와 규칙 일치.
  3. 데이터 0행 라인에서 `fit_final`/`benchmark`가 sklearn 난해한 오류로 crash → `forecast.InsufficientDataError` + `require_rows()` 도입, **최소 20행 미만이면 지어내지 않고 명시적 거부**(CLAUDE.md §2-1). `train_and_save`·`benchmark_models`·`make_final_report`·`monitor`가 모두 사유를 안내하고 건너뜀.
  4. 소표본에서 **KNN `n_neighbors`(7) > 학습폴드 크기** → CV 폴드에 맞춰 자동 축소.
  5. `monitor_line` 데이터부족 조기반환이 **series 스키마를 깨뜨려** `KeyError: 'actual'` → 빈 `datetime/actual/pred` 프레임 유지(`_insufficient()`), `MIN_ROWS`를 학습 최소행수와 연동.
  6. 리포트 탭이 **라인 2개 고정 인덱싱**(`bench_secs[1]`)으로 `IndexError` → `_expand()`로 가변 처리 + 없을 때 사유 안내.
  7. 빈 기간에서 `NaTType does not support strftime` → NaT 안전 처리.
  8. 대시보드가 데이터 없는 라인에 **`nan%` 노출** → `-` + "데이터 없음"으로 정직 표기.
  9. 대시보드 종료일이 **+1일 보정**되어 사이드바 표기와 불일치 → 제거(2번과 규칙 통일).
  · **검증**: 엣지케이스 4종(전체·기존라인 없는 기간·하루만·데이터 없는 미래) 전부 통과, `make_final_report` 4개 기간 정상 생성, **Playwright로 대시보드 4탭 + 리포트 7탭·날짜 직접입력(적용) 실렌더 확인**(무데이터 구간 `-` 표기).
  · 회귀 테스트 `tests/test_edge_cases.py` **15개 추가 → 총 58개 통과**.
- **[데이터 갱신]** (2026-07-28): `data_v1.xlsx` 최신본 반영 — 구조 동일(8시트), 기간 07/22 → **07/28** 확장.
  · 증가: OSP인출 정제 455→**599행**, CNA 20,952→**24,601행**, 야드변경 31→**33건**(신설 +2). 광산·45Q·기존야드변경은 동일.
  · 검증 게이트 **ERROR 0 · WARNING 0**. 전체 파이프라인 재실행 완료(run_all).
  · 재벤치마크: 신설 MAE 0.798→**0.790**, **Ridge가 신설 라인 1위로 상승** → 확정 모델 Ridge 유지 타당성 재확인.
  · ⚠️ **운영 변화**: 신설 라인이 🟡주의 → **🔴경고**(최신 CaO 46.69%, 19시간 연속 규격 이탈). 두 라인 모두 경고 상태.
- **[그래프 내 기간 선택]** (2026-07-25): 시간축 차트(타임라인·추이·관리도·예측·경보이력) 우측 상단에 **기간 버튼(1주/2주/1개월/전체) + 하단 레인지 슬라이더** 내장(`figures._time_range_controls`). 사이드바 없이 그래프에서 바로 기간 확대.
- **[기간 설정 + 표준편차]** (2026-07-24): **대시보드 사이드바 날짜범위 필터**(모든 차트·std 선택구간 재계산) + **정적 리포트 기간 인자**(`make_final_report.py --start --end`). 표준편차 3종: 야드별(변경)·라인별(변경)·**라인별 연속측정(CNA/45Q)**. 인사이트: 연속 실측 std 1.85~2.26 ≫ 변경데이터 0.5대 → 실제 변동성이 큼. (`figures.yardchange_std_summary(level)`, `continuous_std_summary`, `dataset.filter_period`)
- **[야드변경 추적]** (2026-07-24): 신규 시트 `기존/신설라인 야드변경`(변경일·시간·Yard·석회석CaO·MgO·야드물량) 분석·정제(`clean.clean_yard_change`, `dataset.load_yard_change`). **야드변경 기반 Sankey + CaO/MgO 버튼 토글**(`figures.build_yardchange_sankey`, 라인→야드 Y1/Y2, 링크=야드물량, 색=성분품위) + **변경일자별 CaO·MgO 추이**(`figures.yardchange_trend`). 대시보드 '추적 흐름' 탭에 통합(+상세표). 차트 x축 날짜 슬래시 형식(2026/07/24)으로 수정.
- **[대시보드 확장]** 3개 탭 추가(2026-07-23): ① Sankey 추적도(`figures.build_tracking_sankey`) ② 관리도/제어차트(`figures.control_chart`, 규격밴드+평균±3σ+규격내 KPI) ③ 경보 이력 로그(`src/monitoring/history.py`, 로컬 CSV 누적·중복제외, 타임라인). 이력 테스트 2개(총 22개 통과). 이메일 알림은 보류(기밀·SMTP).
- **[운영도구] 모니터링·경보 시스템** (2026-07-23):
  - `src/models/dataset.py`(공용 로더), `src/models/persist`→`scripts/train_and_save.py`(Ridge 라인별 학습·저장 models/*.joblib)
  - `src/monitoring/alerts.py`(규격이탈·지속·모델편차·추세 경보, 설정가능) + `src/monitoring/monitor.py`(최근구간 예측·상태)
  - `scripts/monitor.py`(CLI → `outputs/monitor_status.html`), **`streamlit_app.py`(운영 대시보드, HTTP 200 기동 확인)**
  - 경보 테스트 5개 포함 총 20개 통과. 현재 데이터 말미 기준 기존=🔴경고(21h 연속이탈), 신설=🟡주의.
  - 새 데이터 오면 재실행/새로고침 = '실시간' 갱신 패턴.
- **[모델선정] 10개 모델 벤치마크** `src/models/benchmark.py` + `scripts/benchmark_models.py`
  - 결과: **선형 계열(LinearRegression/Ridge/ElasticNet)이 최적**. 신설/CNA MAE 0.798, 기존/45Q 1.032.
  - XGBoost/LightGBM/RF 등 복잡모델은 소표본 과적합으로 열위 → **운영모델 Ridge 확정**(강건).
  - 리포트에 벤치마크 차트 반영(통합 리포트).
- **[파일럿] 배합최적화 스캐폴드** `src/optimization/blend.py`(scipy linprog, 달성불가시 근접해+병목), 테스트 4개
- **[파일럿] 예측모델** `src/models/forecast.py`(Ridge AR, 라인별, 시계열CV)
- 프로젝트 저장소 초기 구조 생성 (data/ src/ scripts/ docs/ 등)
- 기존 Streamlit+OpenAI 챗봇 스캐폴드 제거
- `CLAUDE.md` (프로젝트 헌법) 작성 + 운영규칙 보강 (성공기준·infeasibility·검증게이트·핸드오프 커밋)
- 데이터 분석/ML 의존성(`requirements.txt`) 설정
- **데이터 로더 뼈대** (`src/data/loader.py`): `inspect_excel`/`inspect_raw_dir`(스키마 미확정 정찰용), `load_source`/`load_all_sources`(스키마 확정 후 로딩). 파일명 미설정 시 지어내지 않고 안전 차단.
- **검증 게이트** (`src/data/validation.py`): CaO 범위(0~100%)·음수/0 물량·Key 결측·Key 중복·공정 시간역전(광산≤OSP≤야드) 검사. ERROR/WARNING 리포트.
- **설정 주도 구조** (`config/paths.py`, `config/schema.py`): 경로 중앙관리 + SourceSpec(MINE/OSP/YARD 컬럼 플레이스홀더) + 목표 TARGET(44.6±0.5).
- **단위 테스트**: 40개 전부 통과 (`python -m pytest tests/`) — 정제·매칭·검증·경보·배합·이력.

## 2. 핵심 데이터 스키마 & 주요 변수 (Schema) — ✅ 확정
원본 `data/raw/data_v1.xlsx` **8시트** (광산 49Q_XRF·47Q_감마 / OSP인출 기존·신설 / CNA / 45Q감마 / 기존·신설라인 야드변경). 상세는 `docs/data_schema.md`.
- **라인 = 서로 다른 야드**: 기존 → 45Q(4-5K 킬른), 신설 → CNA(6-7K 킬른). *(사용자 확정)*
- **P/W1~4호 = 인출 지점**(품위 아님), 지점당 **50% 균등 배분**. 기존→P/W1·2, 신설→P/W3·4.
- **품위 매칭 = 시간인지**: 인출시각 이전의 최근 적재 품위(merge_asof), 라인 교차 금지.
- **Time-Lag**: 데이터로 추정(상관 최대), 라인별 6~24h 범위.
- **야드변경 시트**: 변경일·Yard(Y1/Y2)·석회석CaO·MgO·야드물량 → 추적/품위 분석의 기준.
- 단위: 물량 톤(ton), 품위 %. 목표 CaO 44.6±0.5.
- 코드 스펙: `config/schema.py`의 `SPEC_MINE/SPEC_OSP/SPEC_YARD/SPEC_YARDCHANGE` (정제 프레임 기준, 검증 게이트가 사용).

## 3. 현재 직면한 문제점 / 미해결 이슈 (Open Issues)
- **[⏳ 최우선 — 2차 현장 문의 회신 대기]** **구역 귀속 붕괴**(§6-0-16)를 푸는 질문을 작성해 사용자에게 전달했다(2026-08-14). **질문 전문·근거 데이터·회신 후 할 일은 `docs/field_questions.md` 에 보관** — 대화창이 사라져도 이어갈 수 있다.
  · 핵심 근거: 구역 **80번은 08-05에 처음 적재했는데 06-11부터 23건 인출**, 90번은 첫 인출이 첫 적재보다 5일 빠름, 95번은 21건 63,138톤 적재에 인출 2건뿐, 100번은 인출 0건.
  · 묻는 것: ① 80·90번 물량의 원래 적재 위치(+ Q4 답변과 데이터가 어긋나는 점 재확인) ② OSP 내 물량 이동 작업 여부(가설: 95번 47Q 물량을 90번으로 옮겨 인출) ③ 인출 기록에 '원래 적재 위치' 칸 추가 가능 여부.
  · ⭐️ **①·②는 규칙만 알면 과거 데이터 전체에 소급 적용 가능** → 파급이 가장 크다. ③은 향후 데이터만 개선.
  · 성공 지표: 반영 후 `make_prescription.py` 재실행 시 **규격 적중 확률 상승**(현재 43~47%, 운전 사용 기준 80%+).
- **[⭐️ 근본 원인 — 구역 귀속 붕괴]** 인출 기록의 구역 번호가 **그 물량을 적재한 곳을 가리키지 않는다.** 기존 라인 17개 구역 중 11개(인출의 55%), 최악 21배. 적재는 95·100번, 인출은 60~90번. **구역 단위 상관이 0.2 를 못 넘던 이유**이며, 처방 적중률이 낮은 직접 원인. `prescribe.zone_balance_health` 로 상시 감시. 상세 §6-0-16.
- **[계량 정합 — 성격 규명됨 2026-08-14]** 종전 '벨트스케일 드리프트' 로 보던 현상은, 현장 답변에 따르면 **광산 계량과 공장(시설 3호) 계량이 서로 다른 기기**이고 **월마감 때 공장이 증량·감량을 정해** 조정하기 때문이다. 즉 **차이가 있는 것이 정상**이며 '손실'이 아니다. β 는 **월 단위로 따로** 추정해야 한다(6→7월 두 라인 동반 하락 확인). 월 이하 해상도는 실사 재고 오차(Q5: 담당자별 편차 있는 개략치)에 묻혀 판별 불가. 상세 §6-0-14.
- **[디자인·종결]** `canvas-design` 기반 재조판(SEDIMENT INDEX)은 **사용자 판단으로 철회**, 이력에서 제거됨. 리포트는 남색 기반 기존 디자인 유지. 재시도 금지(§상단 경고 참조).
- **[본질적 한계]** 상류(OSP 인출) → 야드 CaO 상관 **0.11~0.16**(설명력 1~2%)로 매우 약함 → 예측은 지속성(AR) 기반, 목표 MAE<0.5 미달. **처방적 배합제어는 신호 강화(데이터 축적) 후 가능.** *(2026-07-28 페어링 버그 정정 후 재계산 — 종전 문서의 0.34/0.39는 뒤바뀐 페어링 기준이었음. `docs/data_schema.md` §7 참조)*
- **[데이터 신뢰도]** 08-05 갱신본에서 47Q 07월 하순 기록이 **통째로 재입력**되고 49Q 품위 18칸이 **삭제**됐다. 현장에서 기록을 정정 중인 것으로 보이나 사유는 미확인 — 추세 해석 시 이 구간은 주의.
  · **47Q 재갱신 평가(2026-08-05 분석)**: 일별 총 물량은 보존(차이 ≤0.4톤=0.005%)한 채 **더 잘게 재분할**한 것. 교대경계 위반 4→1건, 블록 평균 6.2h→3.8h, 품위가 반올림값→계산 원값(소수 14자리), 구역코드 세분화 → **형식·해상도·정밀도 모두 갱신 후가 우수**. 격리 A/B(47Q 시트만 교체, 07/27 절단)에서 신설 상류 상관 +0.162→**+0.181** 개선, 모델 MAE는 불변. **갱신 후 채택이 타당.**
  · ✅ **정정 반영 완료 (사용자 확정 "갱신 전 기준이 맞다", 2026-08-05)**: 갱신 전 기록에 **시간 겹침 다수결**로 대응시켜 4개 블록만 되돌리는 정정표 `S.MINE_LINE_OVERRIDES` 를 도입(시각·라인 라벨만, 품위·물량은 원본 그대로). 결과 07/22~07/27 기존 47Q 15,007→**7,797톤**, 재고 불일치 기존 +16,738→**+9,527** / 신설 −10,844→**−3,634톤**, 신설이 다시 적재>인출이 되어 **모순 해소**. 상세 `docs/data_schema.md` §6-0-7. ⚠️ 원본 `공정구분` 이 현장에서 바로잡히면 이 표를 지울 것.
  · (배경) **라인 귀속이 실사와 배치되던 문제**: 재갱신이 **7,834톤을 신설→기존으로 재귀속**(기존 7,173→15,007톤, 2.1배)했다. 사용자 확정(아래)으로 인출=0 인 깨끗한 창이 생겨 `Δ재고=적재` 로 감사한 결과, **갱신 전 배분이 실사에 더 가깝다**(기존 불일치 +8,966 vs 갱신 후 **+16,738**톤). 갱신 후는 신설을 적재<인출(−1,844톤)로 만들어 **재고가 +9,000톤 늘어난 실사와 모순**되고, OSP1 용량 여유(약 13,000톤)로는 갱신 후 적재 26,738톤을 담을 수 없다. **총 물량·총 불일치는 두 버전 동일** → 순수한 배분 문제. 상세: `docs/data_schema.md` §6-0-6.
- **[✅ 확정 — 2026-08-05 사용자]** **07/21~07/28 기존 라인은 가동하지 않았고 OSP 재고만 채웠다.** `OSP인출_기존`의 07/20→07/29 공백은 **기록 누락이 아니라 실제 미가동**(인출 0 이 참값). → 이 구간은 인출·벨트스케일 오차가 없는 **깨끗한 검증창**이며, β 는 분모 0으로 원리적 식별 불가(§6-0-5 가드가 자동 제외). `S.LINE_NOTES` 갱신.
- **[🐞 발견·수정] 복합 라인 라벨로 21,065톤 누락**: 광산 `공정구분` 의 `운휴/기존`·`기존/신설` 같은 복합 라벨이 라인명과 달라 **라인별 집계에서 통째로 빠지고 있었다**(7행 21,065톤 = 광산 총량 1.7%). 갱신 전후 파일에 **동일하게 존재**한 기존 결함. `clean.split_line_label` 로 전개(운휴/X→X 전량, 기존/신설→균등배분)하고 **검증 게이트에 `line_unknown` ERROR 검사**를 추가해 재발을 막았다. 회복 후 광산 라인별 기존 265,041→**280,234** · 신설 920,990→**926,862톤**.
  · ✅ **사용자 확정(2026-08-05)**: `운휴/기존`·`운휴/신설` = **교대의 절반은 운휴, 절반만 이송**(물량은 전량 실제 라인, 가동 시간만 50%) → 정제 결과에 `active_ratio` 컬럼 신설(`S.IDLE_ACTIVE_RATIO=0.5`). `기존/신설` 1행(5,762톤, 구역 `45/70`)의 **균등 배분도 맞다**고 확정.
  · 정합성 확인: 복합 라벨 6행을 4시간 가동으로 환산하면 473~1,191 t/h 로 전부 능력 상한 내(8시간으로 보면 236~596 t/h 로 비정상적으로 낮음).
- **[데이터 정확도]** 위 창에서 **총 적재가 실사 Δ재고보다 +5,900톤 많다**(양 버전 공통, Δ재고 19,000 대비 +31%). 라인 배분과 별개인 **기록 정확도 문제**. β 보정을 적용하면 인출이 더 줄어 오히려 불일치가 커지므로, 원인은 인출 계량보다 **광산 적재 기록 쪽**일 가능성이 높다 — 현장 확인 필요.
- **[✅ 확정 — 2026-08-13 사용자]** **교차인출 라인 귀속 = 해석 ⓐ** — `기존->신설 교차인출` 등 비고 표기는 **목적지 메모**이고, 인출 주체는 **그 기록이 실린 시트**다. 현행 구현이 이미 ⓐ이므로 코드 변경 없음(`clean.clean_osp` 가 시트 identity 로 라인 주입). P/W 컬럼 사용 실태와도 일치. → 종결. 상세 `docs/data_schema.md` §6-0-13.
- **[✅ 종결 — 현장 질문 5건 전부 회신]** Q1 구역별 측정 없음(6/10 기존 36,000·신설 30,000) / Q2 칸 넘침으로 옆 칸 유입, 잔량 추정 / Q3 시설3호 벨트스케일·월마감 조정 / Q4 90·100번은 47Q 물량, 만실 47,000톤 / Q5 49광구 CCP 산정 개략치. 상세 §6-0-14·§6-0-15.
- **[⛔ 불가 판정 — 구역별 기초 재고]** 인출의 13.1%(187,150톤)가 대체값인 문제는 **구역별 재고를 측정하지 않으므로 원리적으로 해소 불가**(Q1). 게다가 칸 넘침으로 구역 라벨 자체가 근사다(Q2). → **메우지 않고 `grade_source` 로 표시**하는 것으로 확정. 정밀도를 실제로 올리려면 **인출 지점 실측**뿐이다.
- **[⚠️ 성분 정합]** 기존 라인 CaO~MgO 상관이 매칭 후 부호 반전(기준선 −0.159 → +0.088, 차이 +0.247). 인출이 특정 구역에 몰려 원본과 다른 조합이 뽑힌 것으로 보인다. **MgO 예측 피처 사용 보류** 중. 신설은 −0.056 으로 양호.
- **[운영]** 연속 실측 표준편차 1.85~2.26 ≫ 목표 0.5 → **변동성 축소가 핵심 과제**(현재 두 라인 모두 규격 이탈 경보 빈발).
- **[데이터]** 원본은 **로컬 전용**이라 새 세션에서 재첨부 필요(§복원 지점 참조). GitHub에는 코드·문서만.
- (보류) 이메일 알림 — 사내 SMTP·기밀 정책 확인 후.

## 4. 다음 행동 (Next Action) ⭐️
- **담당 Agent**: `[Main-Agent]` → 상황에 따라 전환
- **즉시 실행**: `python scripts/run_all.py` (전체 리포트) · `python scripts/make_brief.py` (현황) · `python scripts/make_prescription.py` (⭐️배합 처방서 — 최종 산출물) (검증게이트→매칭→학습→모니터→통합리포트 원클릭). 대시보드는 `streamlit run streamlit_app.py`.
- **⏳ 대기 중(최우선)**: **2차 현장 문의 회신** — 질문 전문은 `docs/field_questions.md`. 회신이 오면 ① 답변 원문을 그 파일에 추가 ② 이동 규칙을 `src/matching/pipeline.py` 구역 매칭에 반영(소급 적용) ③ `make_prescription.py` 재실행해 적중 확률 변화 확인.
- **후보 작업**: ⓪ **월마감 계량 조정값 확보 요청** — 공장이 정하는 증량/감량 수치를 받으면 β 를 추정하지 않고 직접 알 수 있다(설비 투자 없이 개선되는 유일한 항목) ① **벨트스케일 교정 결과 회신 대기** — 기존 라인 β 드리프트(0.855→1.040)를 현장에서 확인 중, 결과 오면 `dataset.calibrate_flow` 보정 로직 조정 ② 데이터 축적 시 `scripts/benchmark_models.py` 재실행 → 최적 모델 재추천 ③ 배합 최적화(경로2) 정밀화 ④ (요청 시) PPTX 임원 덱 ⑤ (요청 시) 데스크탑 ZIP 번들 최신본 재생성.
- **하지 말 것**: 리포트·차트 **디자인 체계 변경**(색·서체·레이아웃 전면 개편). 2026-07-31에 시도했다가 사용자 판단으로 전량 철회했다. 요청이 있을 때만 착수한다.
- **품질 기준**: 새 기능 추가 시 `tests/test_edge_cases.py` 관례대로 **빈 기간·가동 중지 라인** 케이스를 함께 테스트할 것(위 버그 9건이 모두 이 조합에서 발생).
- **주의**: 데이터·모델·산출물은 **원격 커밋 금지**. 커밋 전 `git status` 확인.
